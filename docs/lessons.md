# lessons.md — キット自身の AIDD プロセス改善ログ

> `templates/lessons.md` は配布雛形（プロジェクト用）。ここは **yuki-aidd-kit を作る作業そのもの**の学び。
> 1エントリ Keep / Problem / Try。数値は実測だけを書く（`rules/absolute-rules.md` A-2）。
> `rules/model-routing.md`「測る」の週1 `/usage` 記録もここに1行で残す。

---

## 2026-09-17 横断 — 全ファイル読解 → spec/ 新設 → 土台（M15）・Pro 移行準備（M16）・デザイン出荷物（M17）

**Keep**
- 先に `spec/` に「読んで分かったこと」を全部書いてから計画した。計画中に再読が要らず、方針決定（Q-1〜Q-9）を1回で済ませられた
- 「Sonnet が壊しても機械が気づけるか」を判断軸にして、テスト（入口 71 / git ゲート 27 / check-docs 25 / check-design 36 ケース）と CI を**先に**入れた。以後の変更で自分のドリフト（INDEX の行数・ケース数・spec の網羅性）を check-docs が毎回拾った
- 「Sonnet に書かせず読ませる」: 散文の CSS 17 ブロックを `templates/ui/components.css` / `layout.css` に実体化し、SKILL.md を 473 → 115 行に。真実源は実ファイル（`tokens.css`）1つにして散文の複製を廃止した
- 1 ステップ = 1 コミットで `spec/` を同じコミットに入れる規約（Q-4）。後追いの同期作業が発生しなかった
- Playwright（同梱 Chromium）でライト／ダーク／360px を実際に開いてから出荷した。3 件の崩れ（topbar 高さ固定・globalbar 折り返し・テーマボタン重なり）は目視でしか見つからなかった

**Problem**
- 見積が大きく外れた: Phase 1（S1〜S6）は見積 50 分に対し実測 12 分。「往復数」で見積もると、スクリプト＋テストの作業は分岐数で決まるので当たらない
- `git clean -fd` が空ディレクトリを消してテストが壊れた／`| head` の SIGPIPE で `set -e` のスクリプトが途中終了した。テストの一時環境の作り方で 2 回手戻り
- 新トークンを tokens.css の 3 ブロック（light / media dark / data-theme dark）に入れるつもりが、1 ブロックに二重投入して data-theme 側が欠けた。**Playwright でトグルした後に `getPropertyValue` を見るまで気づかなかった**
- check-design の `alert()` 検査が `function confirm(` の定義を誤検出した。正規表現だけの検査は「定義」と「呼び出し」を区別できない
- セッションのモデルが途中で切り替わった（保守者の `/model`）。計画は「実装は Opus」だったが確認手段が `get_session` しか無い

**Try**
- [ ] 見積は「往復」でなく「分岐数 × 1 ケース」で出す（テスト系）／「ファイル数 × 読み書き」で出す（文書系）。次の計画で試して実測と比べる
- [ ] テストの一時環境は `mkdir -p` を `reset` 関数に含める。`set -e` のスクリプトで `| head` を使わない
- [ ] 複数ブロックへ同じ値を入れる変更は、入れた直後に `grep -c` で件数を確認する手順を `retro` / `done-gate` に足すか検討
- [ ] 移行後1週間: `/usage` の内訳（スキル・MCP・サブエージェント・長コンテキスト・キャッシュミス）をここに1行ずつ残し、Opus の3条件（Q-7）と U-1〜U-5 を実測で埋める
- [ ] `/context` を S7 前後の状態で測って `spec/11` U-2 / U-3 を推定から実測に置き換える（保守者の手元でしか実行できない）

---

## 2026-09-17 ユースケース検証 — 「社内図書館の貸出管理を Excel から Web へ。HTML でモック」

保守者から実際の依頼文をもらい、キットの手順どおりに作って「キットが本当に使えるか」を確かめた（`init-project.sh library-loan html` → `export-project.sh` → design-system + single-html-tool でモック → `check-design.sh` → `uiux_review`（Playwright で 14 状態）→ `done-gate`）。

**Keep**
- 依頼文 1 行から、規律（目的 1 行・残課題・未検証）と手順書（design-system / single-html-tool / uiux_review / done-gate）が意図どおり効いた。モックは tokens / components / layout / feedback / icons を**貼るだけ**で 5 画面が揃い、CSS を書き足したのは配置の 20 行だけ
- `uiux_review` の「全状態を実機で開く」を Playwright で機械化したことで、目視だけでは出ない不具合（360px の横スクロール、hidden が効かないエラー印、KPI カードのズレ）が 3 件出た
- 「作ったら check-design を叩く」を最初にやったおかげで、**キット側の欠陥（F-14）を配布先の最初の 1 回で踏めた**。出荷物のセルフテストだけでは配布先の形（`.claude/templates/`・貼り込み）は検証できていなかった

**Problem**
- `check-design.sh` は配布先で NG=426 を出した（tokens.css の場所・`.claude/` 走査・貼り込んだ定義行）。キット内の 7 ファイルに対しては NG=0 だったので気づけなかった。**「出荷物が自分で通る」と「配布先で使える」は別のテスト**
- `layout.css` の globalbar は demo-shell.html の短い文言では崩れず、実案件の文言（「社内図書館 貸出管理」＋「総務部 図書係」＋テーマ）で初めて 360px を超えた。デモは**現実的な長さの文言**で作る
- 自分の app.css の `.card + .card` が KPI 列の中にも効いた。部品のクラス（`.card`）に対する隣接セレクタは骨格の中で誤爆する

**Try**
- [x] `test-check-design.sh` に「配布先の形」のケースを追加（ケース10）→ 43 ケース
- [ ] 他の検査スクリプト（check-docs は対象外、trace-check / quality_harness）にも「配布先で `.` を渡した形」のテストがあるか確認する
- [ ] demo-shell.html の文言を実案件相当の長さにする（globalbar / topbar）
- [ ] `references/components.md` に「`.card` への隣接セレクタは `.app-content >` で限定する」を足す

## 2026-09-19 工程承認ゲート（M18） — 「要求したものが確実に作られているか」を機械で止める

**Keep**
- **既にある仕組みの一般化から入った**。`.ui-verified`（画面テストの合格を git HEAD ＋ 内容ハッシュ ＋ 時刻で縛る）が
  そのまま工程承認の形をしていた。新しい概念を発明せず `phase-hash.py` に一般化したので、設計判断が 1 つで済んだ
- **機械と人間の境界を先に文章で固定した**（`approvals/README.md` の表）。以後の実装で迷いが出るたびにこの表に戻れた。
  「AI が承認する」実装を書きかけて止められたのもこの表のおかげ
- **他リポジトリ（qa-autopilot）からはコードでなく作法だけ持ち込んだ**: 3値判定＋解消の検証方法必須（personas）、
  exit 0/1/2 と判定不能を合格に数えない（004-qa-status-gate）、再レビューは差し戻した役のみ・最大2ラウンド（loop.md）
- **配布した実プロジェクトで端から端まで実際に動かした**（未承認→deny／承認→許可／成果物の変更→失効→再び deny）。
  userguide に載せた deny メッセージは作文ではなく実測の出力

**Problem**
- **`covers` に追跡表を含める設計を書いて、テストを書いている最中に致命的な欠陥に気づいた**。追跡表は全工程が
  書き足す生きた文書なので、第3工程の更新が第1・第2工程の承認を毎回失効させ、ゲートが永久に詰まる（F-19）。
  「承認を版に縛る」を機械的に適用すると、**版が動き続ける文書**で破綻する
- 新スキル 1 本・新コマンド 1 本を足しただけで、スキル数（19→20）・コマンド数（16→17）の記載が
  `test-install.sh` / `INDEX.md` / `userguide.html` / `spec/` の 7 箇所に散っていた。`check-docs.sh` が拾うのは
  参照コストとケース数だけで、**件数の記載は検査対象外**だった
- 承認記録の「着手判定」に成果物の `YYYY-MM-DD` プレースホルダを使った。雛形が各ファイルにちょうど1つ持つ前提で、
  **雛形を編集すると壊れる**。今は `check-docs` も `trace-check` もこの前提を検査していない

**Try**
- [ ] `check-docs.sh` に「件数の記載 ↔ 実ファイル数」の検査を足す（skills / commands / hooks / rules）。
      今回 7 箇所を手で直した。次に資産を1本足したときに同じことが起きる
- [ ] 「版に縛る」対象を決めるときは、**その文書が誰の書き込みで変わるか**を先に列挙する。
      単独工程が所有する文書だけが対象になる
- [ ] 着手判定の前提（`YYYY-MM-DD` が各雛形にちょうど1つ）を `test-check-approval.sh` の assert に足すか、
      判定方法を雛形非依存にする
- [ ] 実プロジェクトで 1 工程を実際に承認し、deny の false positive 頻度をここに記録する（保守者）

**実測**: 見積 95 分に対し実測は約 70 分（S16〜S22、検証周回込み）。前回の Try「分岐数 × 1 ケース」で
`check_approval.py` を 7 検査＝22 分と見積もり、実測 20 分台。テスト系の見積はこの出し方で当たるようになった。

## 2026-09-19 トークン節約の機械化（M19） — 散文を hook・設定・検査に落とす

**Keep**
- **一次情報から始めた**。hooks / costs / model-config / env-vars を当日取得し、「PreToolUse は `updatedInput` で書き換えられる」
  「PostToolUse は出力を書き換えられない」「Sonnet 5 は effort だけがレバー」「Sonnet 5 の 1M は放置すると 967K まで圧縮されない」を
  確認してから設計した。記憶で書いていたら PostToolUse で絞ろうとして無駄にした
- **棚卸し表を先に作った**（機械 3 つ／散文 9 つ）。何を機械化するかの判断が表 1 枚で済んだ
- **絞った出力の終了コードを保つ**設計（一時ファイル＋`exit $__rc`）。公式例は終了コードが grep のものになる。偽の pytest で
  「102 行 → 7 行、exit 1 保持」を実際に走らせて確かめた
- hook 個数の assert をリポジトリ実体から導出する形に変えた（`test-install.sh`）。以後 hook を足しても数字を直さなくてよい

**Problem**
- `token_audit.py` の床の推定が、キット本体では `@AGENTS.md` を解決できず（実体は `.template`）3,800 tok と過小に出た。
  配布先で 5,800 tok と出て気づいた。**計算対象の実在を前提にした推定は、配布形と本体形の両方で走らせないと嘘をつく**
- 案の `covers` 問題（M18）と同型: 「配布先では効くが本体では効かない」差分は、export した一時ディレクトリで実際に回すまで見えない
- 警告系 hook 3 本の stdout が Claude に届いていない疑いが残った（F-21）。exit 0 の stdout がどこに出るかを、
  hooks docs を読んだ時に確認しなかった

**Try**
- [ ] F-21: hooks docs で「exit 0 の stdout の行き先」を確認し、Claude に読ませたい警告は `systemMessage` に載せ替える
- [ ] 推定値を出すスクリプトは、テストで「本体」「配布先」の両方を走らせ、差が出たら理由を assert する
- [ ] 移行後 1 週間: `/usage` の「キャッシュミス」「長コンテキスト」フラグ、`token-audit.sh` の床、`instructions-loaded.log` の集計を週次表に残し、
      `READ_GUARD_MAX_LINES`（800）`CONTEXT_GUARD_IDLE_MIN`（55）`effortLevel`（high）を実測で見直す

**実測**: 見積 82 分に対し実測 約 65 分（S23〜S29）。Opus/Fable で書いたのは S23 だけ、他は Sonnet 相当の定型作業として進んだ。

## 2026-09-19 テスト工程のメトリクス（M20） — 表を埋めれば機械が数える

**Keep**
- **根本原因（実行記録が機械可読でない）を先に固定した**。列と語彙を決めた S30 が 10 分で終わり、S31 の設計判断は「合格に数えてはいけない状態」の列挙だけになった
- **qa-autopilot の作法をそのまま持ち込んだ**: unread を分母に入れる・None と 0 を区別する・推定に根拠を付ける・status と gate を分ける。
  自分で考え直していたら「unread は落とす」を選んでいた可能性が高い（消化率が上がって気持ちいいから）
- **事例で端から端まで回した**ので、配布スクリプトの同梱漏れ（F-23）と `--into` の日付ズレが実装のうちに出た。
  テストは全部通っていたのに、配布先では 1 行も動かなかった

**Problem**
- 新スクリプトを足したときに `export-project.sh` / `init-test-docs.sh` の同梱リストと `test-install.sh` を更新する手順が**横断ルールに無かった**。
  M19 でも同型のミス（本体形と配布形の差）を lessons に書いていたのに、リストの更新は仕組みになっていない
- `--gate` で「unread がある」を最初は基準 4 の ✗（進めない）として扱っていた。テストを書いていて「指標そのものが確定していないのに『進めない』と言うのは嘘」だと気づき 2 に直した。
  **判定できないと進めないは違う**。混ぜると人が「直せば通る」と誤解する
- スキル・コマンドの個数を `test-install.sh` にハードコードしていたので、コマンドを 1 本足しただけで 2 ケース落ちた（hooks は M19 で導出に変えていたのに、こちらは残っていた）

**Try**
- [ ] `check-docs.sh` に「`scripts/*.py` `scripts/*.sh` のうち配布対象（テンプレートから参照されるもの）が `export-project.sh` の同梱リストにあるか」の検査を足す
- [ ] 単体テストの結果 XML（pytest / vitest）から 05 の表へ自動転記する経路を検討（`filter-output.py` の集計行と接続）
- [ ] 実案件で 1 工程分の表を実際に埋め、`--gate` の 2 が邪魔になる頻度（語彙外の書き方の癖）を記録して語彙を見直す

**実測**: 見積 80 分に対し実測 約 75 分（S30〜S34）。S31 の 40 アサーションのうち 4 件が最初に落ち、うち 2 件は仕様の誤り（unread の扱い）、2 件は fixture の見落とし（CSV の雛形が混ざる）だった。

## 2026-09-19 保守者の傾向の反映（M21）

**Keep**
- 保守者が**別環境の AI に毎回貼っている規約**（qa-autopilot の PROMPT_FOR_CODEX・CLAUDE.md）が最も信頼できる一次資料だった。
  「毎回貼る」ということは、それが守られないと困る線そのもの
- 本セッションの指摘（「止まっている」「感想はいらない」「損益で判断して対応して」）を原文のまま表に残した。要約すると角が取れて規約にならない
- 傾向 6「数値は実体から算出」は検査 10 として機械化した。入れた直後に userguide の hooks 数（7 ↔ 13）を拾い、
  手で 2 回直した箇所がもう 1 箇所残っていたことが分かった

**Problem**
- A-11「止まらない」を作る原因になった場面（tag push 403）は、A-8「放置しない」があっても防げなかった。
  A-8 は「動かしたものを見張る」規範で、「塞がれたら代替を取る」は別の規範。**既存ルールで拾えると思い込まず、落ちた場面から規範を起こす**
- 検査 10 の正規表現は「スキル/コマンド/hooks の直後 4 文字以内の数字」に限定した。「DAILY スキル 12 本」のような部分集合を実数と突合して
  誤検知する余地があるため WARN 止まり。NG に上げるには件数を書く場所を決め打ちにする必要がある

**Try**
- [ ] 保守者から同じ指摘を 2 回受けたら `docs/maintainer-tendencies.md` に行を足す（`/retro` の手順に 1 行入れる）
- [ ] 検査 10 の誤検知が 1 か月出なければ NG に昇格

### 第 2 回（同日。「やはり浅すぎる」）

**Problem**
- 第 1 回は「保守者が AI に言った言葉」だけを拾い、「保守者が実装者に課している手順」（loop.md の 4 行・5 つの質問・止まる条件・予実表、
  WORK_ORDER の守ること表、VERIFICATION_REPORT の 30 ペルソナ）を読んでいなかった。**読んだ範囲の中で傾向を出しても、読んでいない記録に一番濃い型が残っている**
- 反映先が `rules`・`AGENTS` の散文に偏った。手順は散文で読んでも実行時に思い出せない

**Try（実施済み）**
- 手順の型は手順が走る場所（`/plan`・`implement-profile`・雛形・`/qa-review`・`done-gate`）へ埋める（Q-15）
- 「作った本人に検証させない」を実行できる形にする: ペルソナ一覧と判定一覧の型（`references/personas.md`）
- [ ] 実プロジェクトで `/qa-review` のペルソナ順次実行を 1 回回し、往復数と拾えた指摘数を記録する

## /usage 週次記録（移行後に追記）

| 週 | 基盤 | スキル | MCP | サブエージェント | 長コンテキスト | キャッシュミス | 上限に当たった回数 | 備考 |
|---|---|---|---|---|---|---|---|---|
| （2026-10 第1週から） | | | | | | | | |

## 2026-09-19 指示優先の機械化（M22） — 指示を読み飛ばした事故から

**Problem**
- 連続コマンド実行中に届いた保守者の指示（「日本語で報告」「中間報告を今すぐ」×2）を、ツール結果の一部として扱い次のコマンドへ進んだ。途中報告は英語。
  「作業を完遂する」「止まらない」を保守者の最新の指示より上に置いた。**散文の規約は作業の連鎖の中では読み返されない**（M19 と同じ結論が、今度は自分に起きた）

**Try（実施済み）**
- A-13 指示優先を規範化し、`instruction-guard.py` が transcript を見て全ツールを止める。deny 理由に指示の先頭を載せる＝読み飛ばしを物理的に不可能にする
- 実 transcript で検証してから配線する（応答前の断面で deny・応答後で許可）。合成データだけで済ませない
- [ ] 1 週間運用し、誤 deny（サブエージェント・スラッシュコマンド・英語の指示）の有無を記録する

## 2026-09-19 Codex hook の確認（M23） — 「無い」と書いた前提を一次情報で覆した

**Problem**
- 「Codex には hook が無い」を M16 以来 5 箇所に書いていたが、確認日も参照元も無く、記憶からの記述だった（A-5）。保守者の「Codex でも使う前提は考慮したか」で初めて一次情報（openai/codex `codex-rs/hooks`）に当たった
- 最初の報告で「11 本そのまま移せる」と言った後、`tool_input` の中身（apply_patch は `{"command": <パッチ本文>}`）と transcript 形式（rollout）を読んで 4 本に訂正した。イベント名と stdin のキー名が同じでも、**値の形が同じとは限らない**
- 報告の目的（報告／連絡／相談）を混ぜて出し、「何を言っているか分からん」を受けた。判断を求めるなら閉じた質問＋推奨の 1 形式に揃える

**Try**
- 外部ツールの能力に依存する記述には「確認日＋参照元（commit）」を添える。無いものは `check-docs` の WARN 候補
- hook の可搬性は「イベント名」でなく「hook が読むキーと値の形」で判定する。判定表は `spec/11` D-4
- [ ] Codex 実機で `.codex/hooks.json` の 4 本を動かし、B-14 の完了条件を実測で埋める
- 「検査が緑＝文書が最新」と取り違えた（F-27）。行数・件数の突合は鮮度を見ない。**変更したファイル名で文書を総当たりし、言及している文書が同じ差分に無ければ止める**（検査 12・docs-gate.py）。入れた直後に自分の差分で 7 文書を拾った
- 指示（「12 原則を守るようにして欲しい」）を実装依頼と受け取らず、許可を 2 回求めた。判断を委ねられたら実行まで行う（AGENTS「応答スタイル」）
