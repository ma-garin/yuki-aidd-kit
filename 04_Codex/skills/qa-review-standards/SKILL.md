---
name: qa-review-standards
description: "AIによるレビュー・テスト・品質評価に国際規格を組み込むスキル。UXレビュー、コードレビュー、テスト設計、AIレビュープロンプトの作成、severity分類、品質スコアリングの依頼があった場合は必ずこのスキルを使用すること。「品質観点で見て」「レビューして」「テスト観点を出して」「ISO準拠で」「ISTQB準拠で」という依頼、およびQA系ツール（UX audit tool / QA Lens等）へのレビューロジック実装にも適用する。"
---
<!-- 生成物: 04_Codex/build_codex_skills.py が 03_ClaudeCode/ から生成する。直接編集しない -->
# QAレビュー規格セット

第三者検証の品質基準をAIレビューに注入する。判定は常にevidence-only（根拠を提示できない指摘は出力しない）。

## ISO/IEC 25010 スコアリング（5カテゴリ運用）
UX/プロダクトレビューでは以下5特性で0〜100スコア＋根拠を出す:
1. **使用性**（操作の学習性・エラー防止・アクセシビリティ/WCAG 2.1）
2. **性能効率性**（応答時間・リソース・容量）
3. **信頼性**（障害許容・回復性・データ保全）
4. **セキュリティ**（機密性・インテグリティ・否認防止）
5. **保守性**（モジュール性・解析性・修正容易性）

出力形式: カテゴリ別スコアカード＋指摘リスト＋改善提案。総合点は単純平均にせず、プロダクト特性に応じた重み付けを明示する。

## ISTQB severity 分類
全指摘に必ず付与:
- **Critical**: データ損失・セキュリティ侵害・主要機能停止
- **High**: 主要ユースケースの阻害、回避策が困難
- **Medium**: 回避策あり、限定的影響
- **Low**: 軽微なUI不整合・改善提案レベル

severityとpriorityを混同しない。priorityは別軸（ビジネス判断）として扱う。

## Whittaker ツアーモデル（探索的テスト観点）
テスト観点生成時は以下から該当ツアーを選んで適用:
- Guidebook Tour（仕様書通りの操作）/ Money Tour（売りの機能）
- Landmark Tour（主要機能間の遷移）/ Intellectual Tour（複雑な入力）
- FedEx Tour（データのライフサイクル追跡）/ Saboteur Tour(破壊的操作)
- Back Alley Tour（使われない機能）/ All-Nighter Tour（長時間稼働）

## ISO/IEC/IEEE 29119-3 のテスト文書
テスト計画・テスト設計仕様（技法適用分析）・テスト完了報告・インシデントレポートの雛形は `templates/test/iso29119-*.md`（配置: `./00_導入/02_プロジェクト配布/init-test-docs.sh <対象>`）。レベル・ゲートの定義は `skills/test-strategy`。システムテストケースはツアー観点と severity を列に持つ `02_共通/ひな形/test/system_test_cases.csv` の形式で書く。

## 多ペルソナ検証（作った本人に検証させない）
実装者の申告を信じない。`references/personas.md` の 16 ペルソナから対象に合う 5〜10 体を選び、**順次**で「壊れている箇所を探せ」と回す（並列にしない）。
出力は判定一覧（番号／ペルソナ／判定 4 値／指摘 1 行）＋集計＋重要度順の指摘（出典はペルソナ番号）。判定不明を合格に数えない。

## 準拠の主張範囲（規格を推測で補わない）
- 条項番号・章構成を出すのは**原本で確認したものだけ**。未確認は規格の名称のみ＋「未確認」と書く
- 「準拠」と書くときは範囲を限定し、対象外を明記する。原本の入手・適用版の確定は依頼者の作業として残課題に書く（`06_保守者向け/02_設計判断の根拠/absolute-rules.md` A-5 補足）

## AIレビュープロンプトへの組み込み手順
1. システムプロンプトに上記3規格の判定基準を明記
2. 「JSONのみで出力、根拠（evidence）フィールド必須」を強制
3. evidenceが空の指摘はパース後に除外
4. イテレーション比較（delta）がある場合は前回スナップショットとの差分を別セクションで出す

## 報告フォーマット
指摘1件 = `{id, category(25010), severity(ISTQB), evidence, 再現手順, 改善案}`。Jira CSV / GitHub Issuesへのエクスポートを想定したフラット構造にする。
Critical/Highは再現手順と実測の根拠が無ければ「未確認」として起票し、重大度を確定しない（`02_共通/ひな形/test/iso29119-incident-report.md` §0 のトリアージに従う）。
AI生成コード・テストコード特有の欠陥観点と指摘の型（`file:line／観点ID／根拠／直し方`）は `references/ai-code-review.md`。

## 適用範囲と引き継ぎ

**次に渡す先**: 画面を実際に開く確認は `uiux_review`、テストの妥当性は `test-strategy`、ペルソナ検証は `references/personas.md`。
