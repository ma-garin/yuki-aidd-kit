# 04 — コマンド（19）と hooks（13 + settings.json）

## 1. スラッシュコマンド

呼んだ時だけコストが発生する明示呼び出し層。1コマンド ≦ 40行が PRD の目安（現状最大28行で充足）。

| コマンド | 行 | 引数 | 実行内容の要点 | 参照スキル |
|---|---|---|---|---|
| `/rfd` | 24 | テーマ（1文） | 課題を事実で書く→**選択肢2つ以上（「何もしない」必須）＋利点/欠点/コスト**→推奨を1つ（決定はしない）→`docs/lifecycle/00-rfd.md` に `RFD-xxx` 追記→**人間の決定を求めて停止**。AI は RFD を承認しない | dev-lifecycle |
| `/lifecycle` | 28 | 工程名 or 番号 or `status` | 工程特定→**入口基準確認（未達なら着手せず差し戻す）**→前工程の成果物だけを入力に生成→追跡表更新＋`trace-check.sh`→出口基準を判定。出力は①工程と入口充足②成果物パス③採番 ID ④trace 結果⑤未達項目 | dev-lifecycle |
| `/trace` | 22 | 対象ディレクトリ（既定 `docs/lifecycle`） | `trace-check.sh` 実行→NG を6分類で対処→NG=0 まで反復。**追跡表を埋めるために要件・テストを発明しない**（カバー漏れは「工程が未実施」のシグナル） | dev-lifecycle/traceability |
| `/plan` | 18 | 概要 or 調査対象 | **`.claude/mode` を削除**（探索許可へ）→grep/glob で絞って調査→判断軸＋選択肢比較で方針合意→`PLAN.md` 生成 | OPERATING-MODE |
| `/implement` | 19 | 補足メモ（任意） | **plan の存在を確認（必須ゲート）。無ければ警告して中断し `.claude/mode` を作らない**→`.claude/mode` 作成→implement-profile 適用 | implement-profile |
| `/compact-work` | 13 | タスク説明 or 空 | 3層要約・探索禁止・スクリプト化を徹底。空なら進行中の作業に適用し直前の冗長出力を圧縮し直す | context-compression |
| `/ecc-daily` | 26 | プロジェクト名/パス | 特定→AGENTS/README/manifest を最小限読む→スタック確認→ECC の DAILY/LIBRARY→Kit 併用スキル。出力は `STACK/DAILY/LIBRARY/NEXT` | ecc-daily-router |
| `/app-scan` | 20 | ワークスペース（既定 cwd） | 除外ディレクトリを明示して拡張子・manifest・README だけ確認→ECC DAILY 候補更新→MAP の更新案。**広範囲の全文読み込みはしない** | — / audit-app-workspace.sh |
| `/sdd-start` | 28 | プロジェクト概要（1文） | ガバナンス3パターン確認→spec.md（FR に検証基準必須・禁止操作明記）→plan.md（比較表）→tasks.md（1タスク1セッション）→CLAUDE.md（3役割） | sdd-ecc-workflow |
| `/new-pwa` | 27 | プロジェクト名＋説明 | spec.md→ディレクトリ構成（最小3ファイル）→index.html スキャフォールド→GitHub Pages 公開手順 | personal-pwa |
| `/qa-review` | 28 | ファイルパス or URL | 読み込み→5カテゴリ 0〜100＋根拠→指摘リスト（**evidence が空の指摘は出力しない**）→ツアー観点（Saboteur / FedEx）→前回スナップショットとの delta | qa-review-standards |
| `/e2e-cycle` | 25 | `1`〜`5` / `help` / 省略 | **`help` は本文表示のみでツール実行・ファイル操作・フェーズ起動を一切しない**。それ以外は1フェーズだけ実行し「ステップN完了」で必ず停止 | e2e-cycle |
| `/eval` | 20 | システム名 / 観点 | 対象特定→スコアラー選定→データセット有無→回帰判定→judge の使い分け（業務=OpenAI / 個人=Gemini・Ollama。比較は同一 judge） | agent-eval |
| `/doc-search` | 14 | 検索語 | キーワード3〜5語→公式→GitHub→その他の優先順→不足ならページ全文取得→規定フォーマットで出力 | code-doc-search |
| `/retro` | 17 | プロジェクト名 or 空 | Keep/Problem/Try 整理→CURRENT_STATE・直近コミット・eval スコア推移を材料に→lessons.md 追記内容→キット反映候補 | retro |
| `/token-check` | 23 | なし | 使用量表示（Claude Code では `/cost`）→読み込み過多の分析→CLAUDE.md 最適化提案→`/clear` タイミング→MCP 見直し（1MCP≈数千トークン） | — |
| `/security-audit`（8.4.0〜） | 25 | 対象ディレクトリ（既定 `.`） | `security-scan.sh` 実行→導入済みの走査器だけで依存/危険なコード/秘密を判定→要旨10行（file:line・**未検査の一覧**・次の一手）。抑制は保守者、AI は修正提案のみ | test-strategy |

### 汎用化の規約（PRD 互換性 NFR）

ツール固有名は「汎用表現（Claude Code では X）」の併記形式に統一されている（AUDIT A-06 で6ファイル修正）。
例: 「検索ツール（Claude Code では WebSearch）」「ページ取得ツール（Claude Code では WebFetch）」「部分編集（Claude Code では Edit ツール）」「使用量表示（Claude Code では `/cost`）」。

---

## 2. hooks

### settings.json の配線（グローバル版・61行）

| イベント | matcher | コマンド | 備考 |
|---|---|---|---|
| `statusLine` | — | `python3 ~/.claude/hooks/statusline.py` | `padding: 2` |
| `PreToolUse` | `Write\|Edit\|MultiEdit` | `bash ~/.claude/hooks/pre-write-check.sh` | 警告のみ |
| `PreToolUse` | `Read\|Grep\|Glob` | `bash ~/.claude/hooks/block-explore.sh` | 実装モード時のみブロック |
| `PreToolUse` | `Bash` | `python3 ~/.claude/hooks/block-gates.py` | `timeout: 10` / `statusMessage: ゲート実行の要否を確認中` |
| `PostToolUse` | `Write\|Edit\|MultiEdit` | `bash ~/.claude/hooks/post-write-html.sh` | HTML のみ反応 |
| `Stop` | — | `bash ~/.claude/hooks/session-summary.sh` | — |

`export-project.sh` が生成する版は同内容を相対パス（`.claude/hooks/...`）にしたもの。
ただし **`Read|Grep|Glob` の block-explore 配線が含まれない**（意図的。`/implement` を使う場合は手で追記）。

### 各 hook の仕様

| hook | 発火 | 入力 | 出力 | 終了コード |
|---|---|---|---|---|
| `pre-write-check.sh` | PreToolUse Write/Edit | stdin JSON `.tool_input.file_path`（jq → python3 フォールバック） | 秘密情報ファイル名（`.env/.pem/.key/.secret`）警告 / 同階層に `.html` がある `.css`・`.js` 書き込み警告（**「単一HTMLツールのプロジェクトなら規約違反。通常の Web プロジェクトなら無視してよい」と条件を明記**＝AUDIT A-07） | 常に 0（ブロックしない） |
| `post-write-html.sh` | PostToolUse Write/Edit | 同上 | `.html` のみ反応。行数/KB 報告 / **500行超で部分編集を推奨** / localStorage 未使用の助言 | 常に 0 |
| `block-explore.sh` | PreToolUse Read/Grep/Glob | stdin JSON `.tool_name` ＋ `CLAUDE_PROJECT_DIR`（既定 `$PWD`） | `.claude/mode` があり tool が Read/Grep/Glob なら stderr に4行の警告 | **exit 2**（ブロック＋Claude へフィードバック）。それ以外 0 |
| `block-gates.py` | PreToolUse Bash | stdin JSON `.tool_input.command` | `hookSpecificOutput.permissionDecision = deny` の JSON。理由に H-7 と `GATES_REQUESTED=1` の指示 | 常に 0（判断は JSON で返す） |
| `progress.py` | 手動（bash に連結） | `start <名> <秒>` / `step <文字列>` / `done` | `.claude/progress.json` の作成・更新・削除 | 0（引数不足で 1） |
| `statusline.py` | statusLine | stdin（そのまま fallback へ渡す） | `⏱ <task> [<step>] <経過>/<見積> 残り <r> ｜ <従来表示>` | 常に 0 |
| `session-summary.sh` | Stop | — | 未コミット件数＋`git status --short` 先頭10行 / `implement.md` が今日未更新なら通知 | 常に 0 |

### block-gates.py の誤検知対策（設計上の要点）

```python
_GATE_PATTERN = (?:^|&&|\|\||;|\||\n)\s*          # コマンド開始位置だけ
                (?:[A-Za-z_][A-Za-z0-9_]*=\S+\s+)* # 環境変数代入列を飛ばす
                (?:venv/bin/)?(?:python3?\s+-m\s+)?
                (?:pytest\b|make\s+(?:test|verify-ui|verify-all|lint|coverage)\b)
```

照合前に**ヒアドキュメント本文**（`_HEREDOC`）と**引用文字列**（`_QUOTED`）を除去する。
これにより `git commit -m 'add make test target'` は素通りする（回帰テスト済み）。
`GATES_REQUESTED=1` がコマンドに含まれていれば無条件で許可。

### statusline.py のフォールバック方針

```text
progress.json あり  → 進捗文字列 ＋ " ｜ " ＋ 従来表示
progress.json 無し  → 従来表示のみ
JSON が壊れている   → 例外を握って従来表示のみ（表示を落とさない）
~/.claude/statusline.sh が無い → cwd のベース名
```

**従来表示を常に消さない**のが設計意図（既存環境を壊さない）。

---

## 3. 回帰テスト（`06_保守者向け/03_回帰テスト/test-hooks.sh` 19ケース）

AUDIT-2026-07 **A-01（Critical）** の再発防止。当時 `settings.json` が
`$CLAUDE_TOOL_INPUT_PATH` という**実在しない環境変数**を使っており、
pre/post hook が `[ -z "$FILE" ] && exit 0` で**導入以来一度も機能していなかった**。
stdin JSON 方式へ修正し、その動作をテストで固定した。

| 対象 | ケース |
|---|---|
| `pre-write-check.sh` | 秘密情報ファイルで警告 / HTML 隣接 CSS で分割警告 / 通常ファイルは無言終了 / 空入力は無害終了 |
| `post-write-html.sh` | HTML 保存後にレポート / 500行超で推奨 / 非 HTML は無言終了 |
| `session-summary.sh` | サマリ出力＋exit 0 |
| `block-explore.sh` | モードOFF で Read 許可 / モードON で Read をブロック（exit 2＋警告）/ モードON でも Write は素通り |
| `block-gates.py` | pytest を deny / `&&` 直後の make test を deny / `GATES_REQUESTED=1` で素通り / 引用文字列内の make test は素通り / Bash 以外は素通り |
| `progress.py` + `statusline.py` | start/step で progress.json 更新 / ステータスラインに ⏱ と経過/見積 / done で削除 |

**2026-09-16 実行結果: PASS=19 / FAIL=0。**
