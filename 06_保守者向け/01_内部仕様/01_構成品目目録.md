# 01 — 全ファイル目録（189件・19,901行）

行数はすべて 2026-09-20 の実測（7.0.0 構成変更後）（`wc -l`）。`06_保守者向け/01_内部仕様/` 自身は対象外。
「役割」は1行要約。詳細は各章（`03`〜`07`）を参照。

---

## ルート（9件・1,043行）

| ファイル | 行 | 役割 |
|---|---|---|
| `README.md` | 167 | 人間向けの入口（7.0.0 で導入に絞った）: 導入2方式・取り扱い説明書・推奨フロー・ECC 連携・構成ツリー・作り込みの入口・合言葉。版歴は `CHANGELOG.md` |
| `CHANGELOG.md` | 330 | 版ごとの変更内容（Ver.5.0〜8.5.0。7.0.0 で README から分離）。版の真実源は `VERSION` |
| `INDEX.md` | 257 | **全資産の索引**。DAILY/LIBRARY 2層＋タグ＋参照コスト。エージェントはまずここを読む |
| `03_ClaudeCode/CLAUDE.md.template` | 32 | `@AGENTS.md` ＋ Claude Code 固有（実装モード・hooks で強制されるもの・トークン/モデル）。共通規約は持たない（M16） |
| `04_Codex/AGENTS.md.template` | 102 | **共通規約の本体**（Codex は直接、Claude Code は import で読む）。速度・必須プロセス・応答・環境・**読む範囲のルーティング表**・完了条件・工程・禁止・コミット・QA（M16）。`.claude/rules/decisions-*.md` は該当ファイルを扱うときに読む旨を追加（B-23） |
| `04_Codex/build_codex_skills.py` | 109 | 03_ClaudeCode の skills・commands から Codex 用スキル（`04_Codex/skills/`・`agents/openai.yaml` 付き・コマンドは `cmd-<名前>`）を生成。`--check` で同期検査。`04_Codex/skills/` は生成物のため目録の網羅性検査の対象外 |
| `.gitignore` | 38 | 秘密情報・ビルド成果物・テスト出力・`.playwright-mcp/`・検査の生成レポートを除外 |
| `.claude/settings.json` | 115 | キット自身の開発セッション用の hooks 配線（instruction-guard / prompt-priority / subagent-context / reply-language / injection-guard を `$CLAUDE_PROJECT_DIR` 参照で）。実体が無ければ無言で飛ばす（`sh -c` の存在確認つき。F-26）。配布形は `hooks/settings.json`（M22） |
| `VERSION` | 1 | 版の真実源（`7.0.0`）。git tag と対応。`install.sh` / `export-project.sh` が導入先の `KIT_VERSION` に刻印 |

---

## 02_共通/rules/（4件・108行）— 規律（`paths` 無し3本＝毎セッション 85行、`paths` 付き1本＝条件付き）

| ファイル | 行 | 役割 |
|---|---|---|
| `absolute-rules.md` | 23 | **A-1〜A-10** を「発動 / 出力 / 要点」の表で（M16 で 112→19 行）。根拠は `06_保守者向け/02_設計判断の根拠/absolute-rules.md` |
| `speed-harness.md` | 59 | **H-0〜H-10** の規範だけ（M16 で 115→51 行、7.1.0 で H-0・H-9・H-10 を追加）。トークン消費の根拠は `06_保守者向け/02_設計判断の根拠/token-economics.md`。出所・失敗事例・実測記録は `06_保守者向け/02_設計判断の根拠/speed-harness.md` |
| `model-routing.md` | 18 | **Pro＋Sonnet の規律**（M16 新設）: 既定 Sonnet・Opus 3条件・effort・`/clear`・委譲・上限時・週1 `/usage` |
| `functional-integrity.md` | 17 | 実行経路を確認するまで「完了」と言わない。**`paths:` 付き＝コード/UI を触ったときだけ読み込み**（M16） |

---

## 03_ClaudeCode/skills/（21スキル・SKILL.md 1,702行 + references 26件 2,282行 + scan.sh 77行）

### DAILY 層（進め方を制御する12スキル）

| ファイル | 行 | 役割 |
|---|---|---|
| `dev-lifecycle/SKILL.md` | 119 | RFD〜保守運用の10工程・ID体系・V字・工程ゲート・役割・委譲表。軽量SDDとの使い分け判断表を冒頭に持つ |
| `dev-lifecycle/references/phase-gates.md` | 139 | 工程0〜9の入口/出口基準チェックリストと差し戻し規則 |
| `dev-lifecycle/references/traceability.md` | 83 | ID 書式・所有ファイル・追跡表の記入規則・trace-check が検出する6種別・GitHub 連携 |
| `qa-review-standards/references/personas.md` | 61 | 検証ペルソナ 16 体（数字の検算係・壊れた入力を渡す人・--help の読み手 等）と判定一覧の型。作った本人に検証させず順次で回す |
| `dev-lifecycle/references/test-levels.md` | 100 | UT/IT/ST/UAT の設計観点と技法（同値・境界・分岐・デシジョンテーブル・FedEx Tour 等）の分担表 |
| `test-strategy/SKILL.md` | 122 | テストレベル L1〜L4・ゲート基準・**ゲートの実行タイミング**・変更タイプ別 DoD・完了基準・29119 対応・機械ゲート一覧。フルゲート表に `/security-audit` を追加（B-24） |
| `test-strategy/references/feature-contracts.md` | 51 | 機能契約 YAML の項目定義と検証8種・運用 |
| `test-strategy/references/ui-verified-gate.md` | 45 | `.ui-verified` の仕組み・BLOCKED 条件・`.rebuild-mode`・配置手順 |
| `e2e-cycle/SKILL.md` | 112 | E2E を5フェーズ（設計→生成→実行→ODC分析修整→コミット）で **1起動1フェーズ**、待機・ポーリング・バックグラウンド完全禁止。画面・状態ごとに `@axe-core/playwright` で serious/critical を FAIL（既存画面は `references/axe-exclusions.md` で期限付き除外。B-24） |
| `e2e-cycle/references/diagnosis.md` | 39 | 修整前の診断の書式（再現手順→失敗シグネチャ→仮説→最小再現→原因箇所 file:line）。`odc_analysis.md` に記録する |
| `e2e-cycle/references/failure-rules.md` | 47 | flaky 判定（単独反復 `--repeat-each=5`）とロケータ破損の修復順（ARIA→role→label→text→testid→CSS）、Playwright エラー文→分類の対応表 |
| `e2e-cycle/references/axe-exclusions.md` | 23 | 既存画面の axe 除外一覧（規則 ID・理由・期限）。期限切れは除外しない（B-24） |
| `context-compression/SKILL.md` | 65 | 3層要約（結論/根拠/詳細はファイルへ）・grep優先・決定論的作業のスクリプト化 |
| `ecc-daily-router/SKILL.md` | 61 | ECC 資産を DAILY/LIBRARY/OFF に分類。プリセットは持たず `01_利用者向け資料/05_ECC資産対応表.md` を参照（AUDIT A-03 で一本化） |
| `sdd-ecc-workflow/SKILL.md` | 61 | SDD 10ステップ・ガバナンス3パターン・役割分離・トークン規律・cc-sdd |
| `sdd-ecc-workflow/references/templates.md` | 56 | spec.md / plan.md / tasks.md / implement.md / AGENTS.md の雛形 |
| `sdd-ecc-workflow/references/ambiguous-words.md` | 44 | 曖昧語の辞書（日: 適切に・必要に応じて・など・高速・十分・可能な限り・迅速・柔軟・使いやすい・わかりやすい 等／英: appropriate, as needed, fast, sufficient 等）。`req-lint.py` の WARN 判定が参照し追記可（B-28） |
| `qa-review-standards/SKILL.md` | 60 | ISO/IEC 25010 の5カテゴリ採点・ISTQB severity・Whittaker ツアー8種・29119 導線・報告フォーマット |
| `qa-review-standards/references/ai-code-review.md` | 33 | AI 生成コード特有の欠陥（もっともらしい未使用コード・存在しない API・過剰な try/except・テスト期待値の逆算・コメントと実装の乖離・依存の綴り違い）とテストコードのレビュー観点。指摘は `file:line／観点ID／根拠／直し方`（B-27） |
| `atarimae-quality-audit/SKILL.md` | 76 | 当たり前品質（Kano must-be）の徹底監査。発見者の作法4条・手順3段・欠陥カタログ11型・Gotchas |
| `atarimae-quality-audit/scan.sh` | 77 | 当たり前欠陥の静的スキャナ（11種の候補検出）。ヒットは候補であって結論ではない |
| `test-automation/SKILL.md` | 66 | 種別別のテスト最小構成（Playwright / pytest / AppTest）と done-gate・pre-commit への接続。Python 側は `pytest-playwright-axe` で同じ axe 基準（B-24） |
| `test-automation/references/playwright_smoke.py` | 53 | PWA/単一HTML 用スモーク scaffold（console error / localStorage / export / 360×820・768×1812 / offline） |
| `test-automation/references/pytest_streamlit.py` | 42 | Streamlit AppTest scaffold（起動・session_state 衝突・LLM モック・マルチテナント越境） |
| `phase-approval/SKILL.md` | 98 | 工程承認レビュー。越えない線（AI は承認しない）・3役を順次で回す理由・出力形式・人間が承認する手順 |
| `done-gate/SKILL.md` | 67 | 完了判定チェックリスト。全種別共通＋変更タイプ別＋工程ライフサイクル＋AI/LLM＋PWA＋単一HTML＋Streamlit |
| `uiux_review/SKILL.md` | 200 | 画面を実際に開いて全状態を確認する手順。適用実数を数える・報告の型・骨格・文言・数字・アイコン・やってはいけないこと |
| `uiux_review/references/viewpoints.md` | 919 | **UI/UX レビュー観点 80件**（VP-001〜VP-080）。9分類（新設「キーボードと支援技術」6件＝Tab順・フォーカス可視・Esc閉じ・矢印移動・読み上げ順・aria-live。B-30）・優先度・25010 副特性・不合格の実例つき |
| `retro/SKILL.md` | 47 | KPT 変形のレトロと `lessons.md` への蓄積、キットへの還流手順。手順の最後に `adr-to-rules.py` の実行を追加（B-23） |

### LIBRARY 層（種別・場面で選ぶ8スキル）

| ファイル | 行 | 役割 |
|---|---|---|
| `design-system/SKILL.md` | 121 | **使い方の規律**（M17 S13 で 473 → 115 行）。何をどこで読むかの索引・トークン運用の規律・骨格・操作には必ず結果を返す・アイコン・文言・画面を作る手順・実物へのポインタ。**値の真実源は `02_共通/ひな形/tokens.css` へ移した** |
| `design-system/references/tokens.md` | 73 | トークンの役割と決めの理由（色の役割表・ダークの作り方・文字7段・余白角丸影動き・フォント読み込み方針・出所）。hex は複製しない |
| `design-system/references/components.md` | 91 | 部品ごとの使い分けと落とし穴（実不具合由来7件を含む: ツールチップ画面端 `.edge-left`・ホバー時のみ表示・確認は対象名を動的に・空状態2用途・フォームエラー2段構え・ダーク rgba・`aria-busy`）。CSS は複製しない |
| `design-system/references/frameworks.md` | 36 | 出荷物（`02_共通/ひな形/ui/`）への導線表＋デザイン系スキルの分担表＋共通の落とし穴。FW 別の置き場所は `02_共通/ひな形/ui/README.md` が正 |
| `nfr-standards/SKILL.md` | 101 | PWA / 単一HTMLツール / Streamlit の非機能既定値（性能・デバイス・オフライン・a11y・セキュリティ・データ保全） |
| `nfr-standards/references/llm-agentic-top10.md` | 39 | OWASP LLM Top10・Agentic の各項目に「キットの対策」「未対応」「確認方法」の表（B-26） |
| `security-audit/SKILL.md` | 82 | 走査器の結果＋スタック別 references（OWASP Top10・ASVS）でセキュリティ監査。手順: security-scan.sh → 未検査の一覧 → スタック別観点 → `security-report.md` へ追記。明示の依頼だけ（`ecc-daily-router` からは自動で振らない）（B-26） |
| `security-audit/references/single-html-pwa.md` | 30 | 単一HTML・PWA スタックの OWASP/ASVS 観点表（キットの対策／未対応）（B-26） |
| `security-audit/references/streamlit.md` | 31 | Streamlit スタックの OWASP/ASVS 観点表（B-26） |
| `security-audit/references/python-web.md` | 31 | Python Web（Flask/FastAPI/Django）スタックの OWASP/ASVS 観点表（B-26） |
| `security-audit/references/node-web.md` | 32 | Node Web（express/fastify/next 等）スタックの OWASP/ASVS 観点表（B-26） |
| `agent-eval/SKILL.md` | 72 | LLM/RAG/エージェントの eval。DeepEval + Langfuse、システム別スコアラー、閾値の決め方、出力形式 |
| `agent-eval/references/eval_dataset_schema.md` | 38 | ゴールデンデータセットの JSONL スキーマと規模の目安・バージョン管理 |
| `agent-eval/references/langfuse_setup.md` | 56 | Langfuse セルフホスト手順と `@observe()` 計装例・無料運用の落とし穴 |
| `agent-eval/references/deepeval_examples.py` | 90 | DeepEval scaffold（G-Eval / Faithfulness / ToolCorrectness / JsonCorrectness と回帰ゲート） |
| `code-doc-search/SKILL.md` | 59 | 技術ドキュメント検索のクエリ最適化パターンと出力フォーマット |
| `single-html-tool/SKILL.md` | 41 | 単一HTMLツールの絶対制約（CSS/JS 外部分割禁止）・デザイン規約・機能パターン・版上げ手順 |
| `personal-pwa/SKILL.md` | 35 | 個人PWA（GitHub Pages・localStorage・Gemini 無料枠・折りたたみ端末 360×820/768×1812） |
| `streamlit-rag-app/SKILL.md` | 37 | Streamlit+RAG 業務アプリ規約。**特定プロジェクト前提**で、一般 Streamlit へは「実装規律」節のみ流用可 |

---

## 03_ClaudeCode/commands/（19件・441行）

| ファイル | 行 | 役割 |
|---|---|---|
| `rfd.md` | 24 | 工程0。課題を事実で書き選択肢2つ以上（「何もしない」必須）→推奨提示→人間の決定で停止 |
| `lifecycle.md` | 33 | 指定工程の成果物生成とゲート判定。`status` で進捗のみ。前工程の ID だけを入力にする |
| `test-metrics.md` | 23 | test-metrics.sh を回し、数字・検知・「人が判定すること」・次の行動を分けて報告。--gate/--history/--into は要求時のみ |
| `phase-review.md` | 26 | 3役を順次実行し `phase-N-ai.md` へ追記。承認欄には触れない。最大2ラウンド |
| `trace.md` | 25 | `trace-check.sh` 実行と NG の分類別対処（C7 suspect を含む）。`--impact`/`--refresh` の使い分け（`--refresh` は保守者だけが打つ）。**追跡表を埋めるために要件・テストを発明しない** |
| `plan.md` | 21 | `.claude/mode` を削除して探索許可 → 調査 → 方針合意 → `PLAN.md` 生成 |
| `implement.md` | 19 | plan の存在確認（無ければ中断）→ `.claude/mode` 作成で探索ブロック ON |
| `compact-work.md` | 13 | context-compression 規約で作業（3層要約・grep 優先・スクリプト化） |
| `ecc-daily.md` | 26 | ecc-daily-router で STACK/DAILY/LIBRARY/NEXT を出す |
| `app-scan.md` | 20 | アプリワークスペースの軽量棚卸し（除外ディレクトリ明示・全文読み込み禁止） |
| `sdd-start.md` | 28 | ガバナンス選択 → spec/plan/tasks/CLAUDE.md を一気に生成 |
| `new-pwa.md` | 27 | 個人PWA の spec〜スキャフォールド（index/manifest/sw）〜Pages 公開手順 |
| `qa-review.md` | 30 | 25010 の5カテゴリ採点＋ISTQB severity＋ツアー観点＋delta 比較 |
| `e2e-cycle.md` | 25 | E2E の1フェーズだけ実行して停止。`help` は本文表示のみでツール実行しない |
| `eval.md` | 20 | 対象システム特定→スコアラー選定→データセット確認→回帰判定 |
| `doc-search.md` | 14 | code-doc-search の規約で検索を最適化 |
| `retro.md` | 18 | Keep/Problem/Try 整理と lessons.md 追記案・キット反映候補。手順の最後に `adr-to-rules.py` の実行を追加（B-23） |
| `token-check.md` | 31 | 使用量確認・読み込み過多の分析・CLAUDE.md 最適化・/clear タイミング・MCP 見直し。`token_report.py --by skill,agent,mcp,tool` の使い方3行を追加（B-22） |
| `security-audit.md` | 30 | `security-scan.sh` を実行し要旨10行で報告（file:line・未検査一覧・次の一手）。AI は修正提案のみ、抑制は保守者（B-24） |

---

## 03_ClaudeCode/agents/（5件・257行）— 自走する実行主体（install で `~/.claude/agents/`、export で `.claude/agents/`。Codex には配らない）

成果物レベルの依頼を受けて工程を自分で駆動する。スキルは基準の真実源として残り、エージェントがそれを読んで実行する（基準を複製しない）。
人間が入るのは要件定義の合意と受け入れ判定の 2 点だけで、工程 2〜7 は各エージェントがループを回して収束させる。
終了条件は既存のゲートスクリプトの exit code に接続する（`trace-check.sh` / `check-design.sh` / `test-metrics.sh` / `check-approval.sh`）。

| ファイル | 行 | 役割 |
|---|---|---|
| `aidd-lead.md` | 79 | 統括。種別判定・進め方の選択・工程の駆動・差し戻しの配分を自分で決める。人間を待つのは工程1と工程8だけ。2周で収束しなければエスカレーション |
| `spec-agent.md` | 50 | 工程2・3。前工程の成果物だけを入力にし（実装コードを読まない）、`trace-check.sh` NG=0 まで自分で回す |
| `build-agent.md` | 50 | 工程4。実装とデザインを分けない。`design-system` の実物を使い `check-design.sh` NG=0 と実行経路の疎通まで自走。画面は全状態を作る |
| `verify-agent.md` | 54 | 工程5〜7。修整前に診断（`odc_analysis.md`）を書き、実行→ODC分析→修整→再実行を Critical/High 残ゼロまで。修整は最小差分に限り、逆適用で赤に戻ることを確認する。アサーションを緩めない |
| `gate-agent.md` | 45 | 各工程の出口。機械判定＋3役レビュー（順次）。差し戻し事項に ID と解消の検証方法を付けて該当エージェントへ返す（診断の無い修整も差し戻す）。`approver` 欄は空のまま |

---

## 03_ClaudeCode/hooks/（25件・5,071行）

| ファイル | 行 | 役割 |
|---|---|---|
| `settings.json` | 228 | 配線定義。`bashOutputMaxChars`・`CLAUDE_CODE_GOAL_CHECKIN_MINUTES=0` を含む。statusLine ＋ PreToolUse(Write/Edit, Read/Grep/Glob, Bash) ＋ PostToolUse(Write/Edit ＋ WebFetch/WebSearch/mcp__*/Read で injection-guard) ＋ SessionStart(compact/resume) ＋ SubagentStart ＋ Stop（B-29） |
| `session-context.py` | 144 | SessionStart（matcher `compact\|resume` のみ。`startup` は対象外）。`CURRENT_STATE.md`（無ければ `.claude/CURRENT_STATE.md`）の「現在の作業」「制約」「次の一手」節から合計12行以内を additionalContext で注入。最終更新から30日超は「古い（YYYY-MM-DD）」を先頭に付ける。ファイル無し・fail-open（B-29） |
| `pre-write-check.sh` | 59 | PreToolUse Write/Edit。書き込む本文に既知形式の秘密値があれば `secret_patterns.py` 経由で **deny**（`AIDD_SECRET_OK=1` で警告に降格）。秘密ファイル名一致・単一HTML の CSS/JS 分割は引き続き**警告のみ**（B-19） |
| `post-write-html.sh` | 34 | PostToolUse。HTML 保存後に行数/KB を報告、500行超で部分編集を推奨、localStorage 未使用を助言 |
| `block-explore.sh` | 40 | PreToolUse Read/Grep/Glob。`.claude/mode` 存在時に **exit 2** で探索を物理ブロック |
| `block-phase.py` | 115 | 未承認の工程の下流成果物への書き込みを deny。`.claude/phase-gate` があるときだけ発動。approvals 配下は常に許可。バイパス無し |
| `filter-output.py` | 114 | 冗長な出力を絞る書き換え（PreToolUse Bash・updatedInput）。テスト→失敗行＋末尾、install/build→tail、git log→-20、git diff→--stat。終了コード保持。`FULL_OUTPUT=1` で素通り |
| `pre-read-guard.py` | 159 | Read/Grep/Glob の前段。読む価値の無いファイルを deny、`READ_GUARD_MAX_LINES`（800）超を `READ_GUARD_LIMIT`（300）に切り詰め。バイナリ・offset/limit 指定ありは素通り。Grep の `path`/`glob`・Glob の `pattern`/`path` も `secret_patterns.py` の秘密ファイル判定で deny（B-19） |
| `instruction-guard.py` | 255 | PreToolUse 全ツール。transcript 末尾を後ろから走査し、保守者の発言の後にアシスタントのテキスト応答が無い／日本語の発言に日本語が無い応答なら additionalContext で通知（deny しない）。サブエージェント（agent_id）・機械由来タグ・transcript 無しは対象外 |
| `reply-language.py` | 415 | Stop。同じ判定で decision=block（stop_hook_active で抑止） |
| `prompt-priority.py` | 78 | UserPromptSubmit。緊急語を含む発言に「作業より優先」を additionalContext で注入 |
| `block-destructive.py` | 507 | PreToolUse(Bash)。取り返しのつかない操作を deny（H-2 の禁止操作＋ push --force ／ rm -rf）。理由に代替手段を載せる。`secret_patterns.py` でラッパーを剥がしてから照合し、秘密ファイルの読み出しコマンドも deny（B-19） |
| `block-protected.py` | 921 | PreToolUse(Write/Edit/MultiEdit, Bash)。`.claude/settings*.json`・`.claude/hooks/`・`.git/hooks/` 等の書き換えを realpath 解決の上で deny。`AIDD_ALLOW_CONFIG_EDIT=1` で解除。判定不能は deny（B-19） |
| `tool-timer.py` | 198 | PreToolUse / PostToolUse。ツールが走っている区間の実時間を積算（並列は二重に数えない）。A-2 の実績の真実源 |
| `subagent-context.py` | 57 | SubagentStart。親への注入が届かないサブエージェントに、保守者の時計・H-4 の受け手側規約・approver 欄を埋めない旨を注入。agent_type が gate/verify/review/qa を含めば「壊れている箇所を探せ」を追加 |
| `context-guard.py` | 59 | UserPromptSubmit。transcript の mtime でアイドル（`CONTEXT_GUARD_IDLE_MIN` 55）・サイズで肥大（`CONTEXT_GUARD_MAX_MB` 4）を判定し additionalContext を注入 |
| `pre-compact.py` | 34 | PreCompact。残す／捨てる／形式の指示を注入 |
| `log-instructions.py` | 35 | InstructionsLoaded。時刻＋入力 JSON をログへ追記。集計は token-audit.sh |
| `block-gates.py` | 54 | PreToolUse Bash。pytest / make test・verify-ui・lint 等を JSON で `deny`。ヒアドキュメントと引用文字列を除去してコマンド開始位置だけ照合（誤検知対策） |
| `block-ci.py` | 57 | PreToolUse 全ツール。自己ウェイク・定期実行のツール（MCP 接頭辞を問わず末尾一致）と Skill loop/schedule、Bash の CI 起動・待機を deny。Bash は `CI_REQUESTED=1` で許可 |
| `progress.py` | 44 | 手動連結。`start/step/done` で `.claude/progress.json` を管理 |
| `statusline.py` | 190 | statusLine。進行中タスクの経過/見積/残りと累計消費（transcript を差分読み、`~/.claude/.statusline-tokens.json` に位置を保存）を表示し、従来表示（`~/.claude/statusline.sh`）へ素通し |
| `session-summary.sh` | 25 | Stop。未コミット件数と implement.md の未更新を通知 |
| `secret_patterns.py` | 898 | **hook ではなく共通部品**（block-destructive / pre-read-guard / pre-write-check / block-protected が import）。秘密ファイル名判定 (a)・秘密値の正規表現 (b)・コマンドのラッパー剥がし (c) を1か所に持つ。`--check-consistency`（他4か所との整合）・`--self-test`・`--pre-write` を CLI で提供（B-19） |
| `injection-guard.py` | 351 | PostToolUse(WebFetch/WebSearch/mcp__*/プロジェクト外の Read)。NFKC・ゼロ幅・双方向制御・base64・URL エンコードを正規化して英日の注入句を検知し、additionalContext で「データとして扱う」を通知して `.claude/injection-guard.log` に記録。**止めない（警告専用・fail-open）**（B-23） |

---

## 00_導入/（13件・1,201行）— キットの checkout から実行する入口（配らない）。01_インストール/・02_プロジェクト配布/・03_点検/ に分ける

| ファイル | 行 | 役割 |
|---|---|---|
| `install-guard.sh` | 5 | 指示優先 3 hook の最小導入（薄いラッパ）。Claude Code 全体に効かせる入口（M22） |
| `install_guard.py` | 134 | 3 hook を `~/.claude/hooks/` に置き、既存 settings.json の hooks に配線だけを merge（冪等・`.bak`・壊れた JSON は触らず exit 1） |
| `install.sh` | 107 | `~/.claude` へ配置（CLAUDE.md・skills・commands・hooks・rules）。既存は `.bak` 退避、rules は同名既存をスキップ |
| `verify.sh` | 401 | 配置確認。**チェックリストをリポジトリ実体から自動導出**（資産追加時の更新不要）。NG>0 で exit 1、版を表示 |
| `export-project.sh` | 208 | プロジェクト配布。`.claude/`（skills/commands/hooks/rules/settings/INDEX/templates）＋`AGENTS.md`/`CLAUDE.md`＋ゲートスクリプト |
| `init-project.sh` | 102 | 新規プロジェクト雛形（pwa / html / streamlit）。.gitignore・CLAUDE.md・CURRENT_STATE・SDD 3ファイル |
| `token-audit.sh` | 7 | トークン監査の薄いラッパ |
| `token_audit.py` | 236 | 床（常時読み込み）の推定 tok・実測ログ集計・hook/設定の配線・MCP 数・スキル肥大。配線漏れは NG |
| `token_report.py` | 360 | セッション JSONL から消費構造を実測（区分別のトークン・構成比・費用比・1 応答あたり出力）。同一 message.id は 1 回だけ数える |
| `install-git-hooks.sh` | 134 | `.git/hooks/pre-commit` に秘密情報スキャンと UI 検証ゲートを 1 コマンドで配線する。`--uninstall` で復元。**Codex でも効く唯一の強制層**（B-14） |
| `init-lifecycle.sh` | 121 | 工程文書11本を `docs/lifecycle/` へ。`--github` で Issue/PR/CI も。既存は上書きしない |
| `init-test-docs.sh` | 37 | テスト文書6本＋CSV＋機能契約＋ゲートスクリプト3本を配置。`--ci` で `test-gates.yml` |
| `audit-app-workspace.sh` | 55 | アプリ群の棚卸し（トップレベル・manifest・拡張子集計・ECC DAILY 推奨） |
| `check_design.py` | 1129 | **デザイン検査の本体**（直値・未定義/未使用トークン・外部 CDN・alert()・tokens.css 読込・フォーカス/動き/モバイル・色名/新色関数/box-shadow/z-index/transition 時間/100vh 等の直値。全 NG に規則 ID `D01`〜）。**D20〜D25 は a11y（`alt` 抜け・ラベル無し入力・`lang` 抜け・空ボタン・見出し飛びWARN・コントラスト比。`contrast-pairs.md` が無ければ WARN）、D26・D27 は AI 既定意匠 WARN（装飾グラデーション・絵文字アイコン。`--baseline` 対象外）**（B-30）。`--baseline`/`--baseline-write` で既知 NG を記録し新規だけ exit 1（**件数が減る方向だけ更新可**）。`--json` で機械可読出力。NG>0 で exit 1。対象は引数（既定 `02_共通/ひな形/ui 02_共通/ひな形/components`）（B-24） |
| `check-design.sh` | 7 | `check_design.py` の薄いラッパ |

---

## 02_共通/ツール/（21件・5,592行）— 導入先の `scripts/` に置かれて動く道具（export-project / init-lifecycle / init-test-docs がコピー。install は check_approval と phase-hash を `~/.claude/scripts/` へ）

| ファイル | 行 | 役割 |
|---|---|---|
| `test-metrics.sh` | 7 | テストメトリクスの薄いラッパ。status / --gate / --history / --into |
| `test_metrics.py` | 729 | 05〜08 の表と CSV から消化率・合格率・欠陥密度・Critical/High 残・偏り・滞留・完了予測（根拠付き）。§7 の基準表で exit 0/1/2。unread は分母に入れ、欠陥表なしは None。`system_test_cases.csv` の「根拠の版」列が現在のハッシュと食い違う PASS は `--gate` で未検証として合格率の分子から外す（消化率には数える）（B-22） |
| `check-approval.sh` | 8 | 工程承認検査の薄いラッパ。`--phase N` / `--gate N` / `--quiet`。exit 0/1/2 |
| `check_approval.py` | 463 | 工程承認の本体。承認記録の必須欄・版の一致（失効）・未解消の差し戻し・未確認事項・approver が人間か・工程順序 |
| `phase-hash.py` | 79 | 工程成果物のコンテンツハッシュ（承認を版に縛る）。対象0件は `empty`。`--staged` あり |
| `trace-check.sh` | 379 | **トレーサビリティの機械検証**。6種別（重複定義/未定義参照/所有ファイル違反/追跡表未記載/カバー漏れ/孤立テスト）＋C7 suspect（`@ハッシュ` の食い違い）・`--impact`・`--refresh`（保守者のみ）。NG>0 で exit 1 |
| `section_hash.py` | 315 | Markdown の節（ID を定義する見出し単位）を正規化（strip・連続空白1つ・空行除去・NFKC）して sha256 先頭7桁を出す。trace-check.sh・test_metrics.py が import（B-22） |
| `adr-to-rules.py` | 346 | 状態=採用の ADR と適用パス付き lessons だけを読み `.claude/rules/decisions-<slug>.md` を生成（frontmatter に `paths:`）。採用でなくなった ADR の出力は削除。retro の手順で手動実行（B-23） |
| `security-scan.sh` | 415 | 導入済みの走査器（pip-audit/osv-scanner/npm audit/trivy/semgrep(ローカル規則のみ)/bandit/gitleaks）だけを実行。無ければ grep シグネチャで「簡易」代替。`security-report.md` に出力。exit 0/1/2（未検査は合格に数えない）（B-24） |
| `quality_harness.py` | 204 | **機能契約ハーネス**。8種の検証（統制文書/列挙値/参照パス/実行経路/失敗系/シンボル/未登録モジュール/未実装マーカー）。NG>0 で exit 1 |
| `ui-hash.py` | 71 | git 管理対象の `.html/.js/.css` の sha256 先頭16桁。`disk` / `staged`。`01_利用者向け資料/` は既定で除外 |
| `pre-commit-ui-gate.sh` | 63 | `.ui-verified` の存在・鮮度（既定7200秒）・UI hash 一致を確認し、UI コミットを BLOCK。`.rebuild-mode` で WARN |
| `pre-commit` | 35 | 秘密情報スキャン。gitleaks があれば使用、無ければ簡易パターン |
| `md-section.py` | 217 | Markdown を見出し単位の節で検索・取り出し（B-21）。`search <語>` は見出しパス・行範囲・推定トークンの表、`get <file>#<見出し>` は節全体を出力。標準ライブラリのみ |
| `baseline.py` | 276 | `check_design.py`・`security-scan.sh --baseline` が共用する基準線の読み書き（`規則ID\t相対パス\t正規化した行` の書式）。件数が増える更新は拒否し、新規の指摘だけを exit 1 にする。標準ライブラリのみ（B-26） |
| `skill-scan.py` | 551 | 外部スキル・MCP・プラグイン導入前の静的検査。`SKILL.md`/`commands/*.md`/hooks の `settings.json`/`.mcp.json`/plugin manifest を判定し `DANGEROUS`（`curl\|sh`・`rm -rf`・秘密ファイル読み・`bypassPermissions`・不可視/双方向制御文字・base64 で隠した命令・外部 URL への POST・`eval` 等）で exit 1、`CAUTION`/`UNKNOWN`（読めない・形式不明。警告して入れる）、`SAFE`。`--json`。標準ライブラリのみ、ネット不使用（B-26） |
| `req-lint.py` | 324 | 要件定義書（EARS 型）の静的検査。非機能要件の数値欠落・列挙数の不一致・ID 重複を NG、曖昧語（辞書は `skills/sdd-ecc-workflow/references/ambiguous-words.md`）と EARS 非該当行を WARN。`.claude/phase-gate` があるとき `check_approval.py --gate 2` の判定に合流（B-28） |
| `cite-check.py` | 292 | ADR・`rules/*.md`・lessons.md の引用（`path#見出し`／`path:行`／`path`）を検査。ファイル無し・見出し無しは NG、`section_hash.py` の節ハッシュが引用時記録（`@版`）と食い違えば WARN（stale）。版の無い引用は件数だけ出し `--record` で記録、`--refresh` で stale を書き換える（いずれも保守者のみ、AI は打たない）。対象は ADR・rules・lessons（skills は対象外）（B-28） |
| `e2e_history.py` | 240 | Playwright の JSON reporter 出力を読み、テスト単位（file＋title）の結果を `.claude/e2e-history.jsonl` に追記（`add`）。`report` で直近 N 回（既定10）の通過率・flaky 率・前回からの回帰を表示。履歴3回未満は「判定不能」で合格に数えない。CTRF・allure 非依存、標準ライブラリのみ（B-31） |
| `pw-spec-lint.py` | 302 | 生成 Playwright テストの locator 規約を grep 型で静的検査。NG: `waitForTimeout`・`sleep(`・理由コメント無し `.only(`/`test.skip(`・旧 API `$()`/`$$()`。WARN: CSS/XPath/`nth()` で `getByRole` 等に置換可能・弱いアサーション・`spec:` ID 無し。`--json`。eslint-plugin-playwright は不使用（B-31） |
| `test-weaken-check.py` | 276 | テストファイルの git diff からアサーション削除・`.skip()`/`.only()` 追加・retries 増加・`toBeTruthy()` への置換を検知して NG。近傍 ±3 行の `weaken-ok: <理由>`（8字以上）で許可。`--staged`（pre-commit 用）・`--base <rev>`（done-gate 用）。標準ライブラリのみ（B-31） |

---

## 06_保守者向け/03_回帰テスト/（48件・7,105行）— 保守者専用: 回帰テストと文書整合検査（配らない。`.github/workflows/kit-ci.yml` が呼ぶ）

| ファイル | 行 | 役割 |
|---|---|---|
| `test-test-metrics.sh` | 307 | test-metrics の回帰テスト（雛形・実データ・unread・欠陥表なし・基準表・偏り・履歴・報告書置換・CSV・根拠の版の未検証・確認待ち/仮置きの --gate 判定。86ケース） |
| `test-token-audit.sh` | 166 | token-audit の回帰テスト（キット自身 NG=0・配布先・配線漏れ・ログ集計・MCP 過多。44ケース） |
| `test-check-approval.sh` | 287 | check-approval の回帰テスト。配布雛形 NG=0 と各検査の NG ケース |
| `test-hooks.sh` | 1488 | **hooks 回帰テスト 904ケース**。AUDIT A-01（hooks が無言で機能停止）の再発防止。B-19 でセキュリティ強制層（block-protected.py・secret_patterns.py 等）、B-23 で injection-guard.py、B-29 で session-context.py（compact/resume 再注入）・hook deny 記録（`.claude/hook-decisions.log`）・statusline の文脈量/使用率を追加 |
| `test-trace-check.sh` | 413 | **trace-check 回帰テスト 94ケース**（C7 suspect・`--impact`・`--refresh` を含む）。雛形が最初から NG=0 で始まることも検証 |
| `test-quality-harness.sh` | 89 | **quality_harness 回帰テスト 11ケース**。雛形契約が新規プロジェクトで PASS することも検証 |
| `test-install.sh` | 459 | **入口スクリプト回帰テスト 209ケース**（install / verify（B64 の paths 0件一致・参照切れ）/ export / init-project / init-test-docs / 承認ゲート / md-section / session-context.py の配線3経路）。HOME を差し替え、実 `~/.claude` には触らない |
| `test-agents.sh` | 108 | エージェント定義の回帰テスト（83ケース）。frontmatter・自走の要件（目標/ループ/終了条件/停止条件）・診断と逆適用の規律・ゲートスクリプトへの接続・Task の保持者を検査。変数への退化を検出する |
| `test-git-gates.sh` | 124 | **git ゲート回帰テスト 27ケース**（pre-commit / ui-hash.py / pre-commit-ui-gate.sh の全分岐を一時 git リポジトリで） |
| `test-adr-to-rules.sh` | 192 | adr-to-rules.py の回帰テスト 54ケース（採用のみ出る・却下は出ない・撤回で消える・paths が入る・slug が安定）（B-23） |
| `test-security-scan.sh` | 215 | security-scan.sh の回帰テスト 63ケース（走査器なし環境の再現・grep 簡易代替・exit 0/1/2・report 表形式）（B-24） |
| `test-skill-scan.sh` | 213 | skill-scan.py の回帰テスト 106ケース（DANGEROUS/CAUTION/SAFE/UNKNOWN の各 fixture・`--json`・install-guard が DANGEROUS で止まり `AIDD_SKILL_SCAN_OK=1` で通る）（B-26） |
| `test-cite-check.sh` | 158 | cite-check.py の回帰テスト 60ケース（ファイル無し/見出し無し NG・版なし WARN＋記録・版違い WARN・`--refresh`・skills は対象外）（B-28） |
| `test-req-lint.sh` | 168 | req-lint.py の回帰テスト 50ケース（NG 3種・WARN 2種・正常な EARS 要件は 0・`--gate 2` 連携）（B-28） |
| `skill_trigger_eval.py` | 207 | スキル発火テストの脚本（正例・負例）。20本の `skill-triggers/*.jsonl` を読み `claude -p … --output-format stream-json` を起動して発火スキルを判定、正例発火率・誤発火率・取り違えを集計。実 API を呼ぶため定期実行・CI には入れず手動起動のみ（B-25） |
| `test-skill-trigger-eval.sh` | 226 | skill_trigger_eval.py の回帰テスト 29ケース（`fixtures/stub-bin/claude` で実 API を呼ばず検証。判定不能・非UTF-8・途中で切れたJSON等の既知の限界を含む）（B-25） |
| `check_docs.py` | 657 | **文書整合検査の本体**（14検査: 参照コスト・掲載漏れ・ケース数・参照切れ・frontmatter（5b agents 含む）・常時読込 rules 行数・行数目安・spec 同期・絶対パス・件数・スキル範囲・資料未反映・安全性）。NG>0 で exit 1 |
| `check-docs.sh` | 5 | `check_docs.py` の薄いラッパ |
| `test-check-docs.sh` | 246 | **check-docs の回帰テスト**（リポジトリ複製に破壊を仕込んで検出を確認。自身が NG=0 で通ることを含む） |
| `test-check-design.sh` | 629 | **check-design の回帰テスト 203ケース**（規則 ID・基準線・新規則6種＋D20〜D27（a11y・コントラスト比・AI既定意匠）・出荷物が NG=0 で通ること＋各検査の検出＋除外規則） |
| `test-e2e-history.sh` | 173 | e2e_history.py の回帰テスト 37ケース（合成 JSON reporter で add→report、履歴3回未満は判定不能、flaky 率、前回からの回帰、壊れた JSON で落ちない）（B-31） |
| `test-pw-spec-lint.sh` | 160 | pw-spec-lint.py の回帰テスト 37ケース（NG 5種・WARN 3種・正当な例は0・`--json`）（B-31） |
| `test-weaken-check.sh` | 225 | test-weaken-check.py の回帰テスト 40ケース（一時 git リポで assert 削除→NG、skip 追加→NG、`weaken-ok` で許可、テスト以外は対象外、`--staged`）（B-31） |

### fixtures/（5件・71行）— token_report.py・skill_trigger_eval.py の回帰テスト用合成データ（実データの構造は同じ、内容は偽物／claude CLI はスタブ）

| ファイル | 行 | 役割 |
|---|---|---|
| `fixtures/transcript-sample.jsonl` | 25 | セッション transcript の合成 JSONL（Skill/Agent/mcp__*/素のツールの tool_use と usage を含む） |
| `fixtures/transcript-sample/subagents/agent-a0001.jsonl` | 4 | サブエージェント transcript の合成 JSONL |
| `fixtures/transcript-sample/subagents/agent-a0001.meta.json` | 1 | サブエージェントのメタ情報（subagent_type 等） |
| `fixtures/skill-trigger-stream.jsonl` | 5 | stub の `claude` が `MARK_*` に応じて返す stream-json 応答の合成データ（B-25） |
| `fixtures/stub-bin/claude` | 36 | `claude` CLI のスタブ（実 API を呼ばず発話中の `MARK_*` から固定の stream-json を返す。test-skill-trigger-eval.sh が PATH の先頭に置いて使う）（B-25） |

### skill-triggers/（20件・120行）— skill_trigger_eval.py の発話ケース（1スキルにつき正例3・負例3。B-25）

| ファイル | 行 | 役割 |
|---|---|---|
| `skill-triggers/agent-eval.jsonl` | 6 | `agent-eval` の発火判定ケース |
| `skill-triggers/atarimae-quality-audit.jsonl` | 6 | `atarimae-quality-audit` の発火判定ケース |
| `skill-triggers/code-doc-search.jsonl` | 6 | `code-doc-search` の発火判定ケース |
| `skill-triggers/context-compression.jsonl` | 6 | `context-compression` の発火判定ケース |
| `skill-triggers/design-system.jsonl` | 6 | `design-system` の発火判定ケース |
| `skill-triggers/dev-lifecycle.jsonl` | 6 | `dev-lifecycle` の発火判定ケース |
| `skill-triggers/done-gate.jsonl` | 6 | `done-gate` の発火判定ケース |
| `skill-triggers/e2e-cycle.jsonl` | 6 | `e2e-cycle` の発火判定ケース |
| `skill-triggers/ecc-daily-router.jsonl` | 6 | `ecc-daily-router` の発火判定ケース |
| `skill-triggers/nfr-standards.jsonl` | 6 | `nfr-standards` の発火判定ケース |
| `skill-triggers/personal-pwa.jsonl` | 6 | `personal-pwa` の発火判定ケース |
| `skill-triggers/phase-approval.jsonl` | 6 | `phase-approval` の発火判定ケース |
| `skill-triggers/qa-review-standards.jsonl` | 6 | `qa-review-standards` の発火判定ケース |
| `skill-triggers/retro.jsonl` | 6 | `retro` の発火判定ケース |
| `skill-triggers/sdd-ecc-workflow.jsonl` | 6 | `sdd-ecc-workflow` の発火判定ケース |
| `skill-triggers/single-html-tool.jsonl` | 6 | `single-html-tool` の発火判定ケース |
| `skill-triggers/streamlit-rag-app.jsonl` | 6 | `streamlit-rag-app` の発火判定ケース |
| `skill-triggers/test-automation.jsonl` | 6 | `test-automation` の発火判定ケース |
| `skill-triggers/test-strategy.jsonl` | 6 | `test-strategy` の発火判定ケース |
| `skill-triggers/uiux_review.jsonl` | 6 | `uiux_review` の発火判定ケース |

---

## 02_共通/ひな形/（48件・3,291行）

### 単体（7件）

| ファイル | 行 | 役割 |
|---|---|---|
| `design-system.md` | 92 | コードを見ずに見た目を再現する視覚的指示書。3パターン（Webアプリ/HTMLスライド/管理画面）＋適用除外＋再現チェックリスト17項目（**機械7 / 目視10** の別付き。M17 S14。a11y・コントラスト比・AI既定意匠を B-30 で追加） |
| `tokens.css` | 116 | **デザイントークンの実物**。ライト＋ダーク（`prefers-color-scheme` と `data-theme` 両対応）、reduced-motion、`--tap-min: 44px`、`--z-base`〜`--z-toast`（B-24） |
| `settings.sandbox.json` | 83 | sandbox（denyRead・network allowlist）＋permissions（allow/deny）の雛形。出所 my-forward |
| `CURRENT_STATE.md` | 60 | セッション間引き継ぎ（フェーズ・直近完了・次タスク・判断待ち・既知の問題・設計決定） |
| `ADR-template.md` | 56 | 設計判断の記録（背景・選択肢比較・決定・影響・関連）。**適用パス・置換先・強制の区分・状態**欄を追加（`adr-to-rules.py` が状態=採用のものだけを rules へ変換。B-23） |
| `lessons.md` | 41 | AIDD プロセス改善ログ（Keep/Problem/Try）。**適用パス**欄を追加（`adr-to-rules.py` の入力。B-23）。現在は雛形のままでエントリ0件 |
| `implement-profile.md` | 34 | 実装モードの行動規範（再探索しない・plan 準拠・小さく実装→軽量テスト・止まる条件・逸脱時の手順） |
| `work-order.md` | 61 | 別モデル（Sonnet / Haiku / Codex）へ渡す作業指示書の雛形（守ること表・Step ごとの完了条件・止まる条件・質問節） |

### lifecycle/（13件・776行）— 工程成果物の雛形

| ファイル | 行 | 役割 |
|---|---|---|
| `00-rfd.md` | 53 | RFD。背景・影響範囲・選択肢表（「何もしない」を含む）・決定・スコープ外・承認欄 |
| `01-requirements.md` | 86 | 要件定義。REQ-F/REQ-N・MoSCoW・観測可能な受入基準・前提制約依存・用語・TBD・承認欄 |
| `02-basic-design.md` | 82 | 基本設計。BD 表・画面設計・データモデル・外部I/F・REQ-N 実現方式・技術選定・リスク |
| `03-detailed-design.md` | 76 | 詳細設計。DD ごとに入出力・処理手順・事前事後条件・**異常系**・**境界値**・状態遷移 |
| `04-implementation.md` | 48 | 実装記録。T 表・作業ログ・設計からの逸脱と逆同期・秘密情報チェック |
| `05-unit-test.md` | 50 | 単体テスト。UT 表（正常系/異常系/境界値の3区分必須）・実行結果 evidence・欠陥 |
| `06-integration-test.md` | 57 | 結合テスト。IT 表・必須観点チェック5項目・モック方針（外部境界まで） |
| `07-system-test.md` | 47 | システムテスト。ST 表（**実測値必須**）・観点チェック6項目・欠陥 |
| `08-acceptance-test.md` | 50 | 受け入れテスト。業務シナリオ単位・探索的テスト1周・不合格項目の判断・**人間の承認欄** |
| `09-operations.md` | 88 | 保守運用。稼働環境・監視 OPS・変更/ロールバック手順（1度実行済みのチェック）・障害切り分け・既知の制約・引き継ぎ順序 |
| `traceability-matrix.md` | 43 | **追跡表の唯一の真実源**。記入規則（空欄禁止・非該当は `-`・REQ-F は UAT 必須・REQ-N は ST 必須）。リンク欄は `REQ-F-001@a1b2c3d` の形に拡張（`@` 以降は上流節のハッシュ。任意。B-22） |

#### lifecycle/approvals/（2件）— 工程承認記録の雛形

| ファイル | 行 | 役割 |
|---|---|---|
| `approvals/phase-approval.md` | 59 | 1工程分の承認記録。covers / reviewed_hash / approver のメタ表・3値判定・条件・根拠・差し戻し事項（解消の検証方法必須）・未確認事項 |
| `approvals/README.md` | 60 | 承認記録の運用。機械と人間の境界表・3値判定・着手判定（`YYYY-MM-DD` の有無）・`.claude/phase-gate` による強制 |

### test/（8件・454行）— テスト活動の雛形

| ファイル | 行 | 役割 |
|---|---|---|
| `TESTING_STRATEGY.md` | 90 | レベル・ゲート・ツールチェーン・**§4 ゲートの実行タイミング**・完了基準・リスク管理 |
| `DEFINITION_OF_DONE.md` | 67 | 変更タイプ別 DoD（A バックエンド / **B フロントエンド★** / C 文書）と PROHIBITED・機械ゲート図 |
| `iso29119-test-plan.md` | 69 | 29119-3 テスト計画。対象/対象外・リスク登録簿・開始/終了/中断/再開基準・環境・役割 |
| `iso29119-test-design-spec.md` | 82 | 技法適用分析（ブラックボックス5技法・境界値の1対1突合・カバレッジ死角・経験ベース） |
| `iso29119-test-completion-report.md` | 75 | 完了報告。レベル別結果・計画との差異・25010 別到達・残存リスク・GO/NO-GO・申し送り |
| `iso29119-incident-report.md` | 59 | インシデント。**製品欠陥/テスト陳腐化（locator 破損／仕様変更）/環境・データ依存/flaky（タイミング／順序・共有状態／通信／乱数・時刻）** の分類と、テストと実装のどちらを直すかの判定。§5 に反復実行結果（n回中m回、単独/並列）の欄 |
| `system_test_cases.csv` | 9 | システムテストケース。列＝ID/ロール/機能/**ツアー観点**/目的/前提/手順/期待結果/severity。6行の記入例 |
| `feature_contracts.yml` | 31 | 機能契約の雛形（JSON 互換 YAML）。harness 設定＋`example_feature`（status: planned） |

### github/（8件・352行）— Issue・PR テンプレートと配布用 CI サンプル `workflows/`（全て手動起動のみ。旧 github-actions/）

| ファイル | 行 | 役割 |
|---|---|---|
| `ISSUE_TEMPLATE/01-rfd.md` | 40 | RFD Issue。選択肢表・決定（人間が承認）・スコープ外・完了条件 |
| `ISSUE_TEMPLATE/02-requirement.md` | 42 | 要件 Issue。種別/優先度・受入基準・由来 RFD・工程の進捗チェック8項目 |
| `ISSUE_TEMPLATE/03-defect.md` | 51 | 欠陥 Issue。検出工程・対象 ID・ISTQB severity・再現手順・**evidence（空なら起票しない）** |
| `pull_request_template.md` | 48 | PR。関係 ID 表・工程・検証結果 evidence・チェック6項目・影響範囲 |
| `workflows/test-gates.yml` | 72 | 機能契約ハーネス＋L1/L2。**Python/Node をファイルの有無で自動判定**、カバレッジ80%ゲート、L3 はコメントアウト |
| `workflows/lifecycle-check.yml` | 42 | 手動起動で `trace-check.sh` を実行。レポートを artifact とジョブサマリへ |
| `workflows/deploy.yml` | 34 | GitHub Pages デプロイ（`01_利用者向け資料/` 配下を公開。手動起動） |
| `workflows/secret-scan.yml` | 23 | gitleaks による秘密情報スキャン（全履歴） |

### components/（4件・618行）

| ファイル | 行 | 役割 |
|---|---|---|
| `feedback.js` | 282 | 操作フィードバックの実装（自己完結・CSS 自己注入）。`ok/error/info/busy/emptyState/confirm`。`textContent` 固定、`javascript:` URL 遮断、押した位置の近くに出す |
| `icons.js` | 152 | Material Symbols（Apache-2.0）同梱。`data-icon` 自動置換、Lucide 名のエイリアス、MutationObserver で描画前に差し込む |
| `demo.html` | 113 | 部品の実機確認ページ。`../ui/components.css` を読み込み、デモ固有 CSS だけ持つ（バッジ／ボタン／入力／KPI／表＋ページャ／トグル／ツールチップ／モーダル／コールアウト／空状態／テーマ切替）。Playwright 確認済み（2026-09-17） |
| `demo-shell.html` | 71 | 骨格の実機確認ページ。`../ui/layout.css` の app 骨格（globalbar / sidebar 折りたたみ・off-canvas / topbar / content）。1366・1920・360px と ダークで確認済み |

### ui/（7件・584行）— デザインシステムの実物（M17 で追加）

| ファイル | 行 | 役割 |
|---|---|---|
| `components.css` | 176 | **部品 CSS の実物**。SKILL.md の CSS ブロックを `var(--*)` だけで1ファイルに実体化（ボタン／入力／バッジ／カード／スコア／KPI／表／列フィルタ／ページャ／トグル／セグメント／ツールチップ／モーダル／通知／空状態／コールアウト／スケルトン／ユーティリティ）。トースト・確認は `feedback.js` の責務 |
| `ui/README.md` | 37 | **どのファイルをどのフレームワークでどこに置くか**の1枚表（単一 HTML / PWA / React+Vite+Tailwind / Streamlit / Flask・Django）＋検証手順 |
| `contrast-pairs.md` | 38 | **トークン対のコントラスト比の対照表（新規）**。`--color-text`×`--color-bg` 等6〜10対をライト／ダークで列挙し、`check_design.py` の D25 が WCAG 相対輝度で比を計算（hex/rgb()/hsl() 対応。oklch は対象外）。対の表が無ければ WARN（B-30） |
| `tailwind.config.js` | 48 | Tailwind `theme.extend`（colors / spacing / borderRadius / fontSize / boxShadow / minHeight tap 等）を CSS 変数参照で登録。値を持たない |
| `streamlit-config.toml` | 12 | Streamlit `[theme]`（tokens.css ライトの写し。値を変えるときは tokens.css を先に直す） |
| `streamlit_theme.py` | 82 | Streamlit へ tokens.css + components.css を1箇所で注入する `apply_theme()` ＋ `badge()` `kpi()` `empty_state()` `callout()`（severity は列挙、`html.escape` 必須） |
| `layout.css` | 104 | **骨格 CSS の実物**。`.app`（globalbar 44px / sidebar 240→200→off-canvas、`.collapsed` 72px / topbar / content）と `.layout-2pane`、KPI 列・フィルタ行・チップ、ブレークポイント 1366 / 768 / 360 |

---

## 01_利用者向け資料/（12件・4,620行）— 利用者向け文書（配布する。export で `.claude/docs/`、install で `~/.claude/docs/aidd-kit/`）

| ファイル | 行 | 役割 |
|---|---|---|
| `01_利用ガイド.html` | 1351 | **初学者向けユーザーガイド**（2026-09-17 新設、同日に「とことん噛み砕く」方針で全面改稿。2026-09-18 に V字・W字章を追加）。18 章: たとえ話と Before/After・先に知る言葉 8 つ・箱の中身・導入前の確認（命令と期待出力）・導入 A / B（1 手順ごとに「なぜ」と「うまくいくとこう見える」）・はじめての会話（対話例 4 つ）・AI との 3 つの約束（ゲートは要求時のみ／未検証を完了と言わない／実装モード）・ハンズオン（事例を通しで・進行役メモ付き）・1 日の流れ・言い方表・品質チェック（**全て手動起動**）・**V字/W字との対応**（インライン SVG 2 枚・工程別の成果物と機械検証の表・W字の未対応 3 点・対外説明の 3 文）・Pro/Sonnet のコツ・見た目・困ったとき（症状→原因→対処）・用語集・次に読むもの。Qiita 風・外部 CDN なし。**デザイン適用除外ジャンル** |
| `02_操作マニュアル.html` | 1824 | 非エンジニア向け HTML 取説。Qiita 風・サイドメニュー追従・用語ツールチップ・13章。冒頭に `01_利用ガイド.html`／事例／V字章への導線（2026-09-18）。**デザイン適用除外ジャンル** |
| `03_ClaudeProjects設定手順.md` | 58 | claude.ai Projects「AIDDラボ」のセットアップ手順（Project Instructions とナレッジ5ファイル） |
| `05_ECC資産対応表.md` | 148 | **ECC 対応表の真実源**。STACK・DAILY 15件・LIBRARY・プロジェクト別 Mapping 5件・install ガイダンス |
| `04_運用モード.md` | 106 | 日常の標準作業モード（種別判定・読む範囲・ECC 使い分け・実装ループ・完了判定・クレジット節約） |
| `01_利用者向け資料/90_サンプル/図書貸出/README.md` | 36 | 事例の説明（依頼文・中身・開く／作り直す／検査する・確認できたこと・範囲外） |
| `01_利用者向け資料/90_サンプル/図書貸出/library-loan.html` | 1239 | **事例の完成品**。単一 HTML（tokens / components / layout / icons / feedback を貼り込み）。`build.py` の生成物。5 画面・localStorage |
| `01_利用者向け資料/90_サンプル/図書貸出/app.css` | 22 | モック固有の配置 CSS（部品・骨格はキットのまま） |
| `01_利用者向け資料/90_サンプル/図書貸出/app.js` | 222 | 画面ロジック（hash ルーティング・貸出／返却・失敗の再現・CSV・localStorage schema v1） |
| `01_利用者向け資料/90_サンプル/図書貸出/build.py` | 178 | app.css / app.js とキットの実物から library-loan.html を組み立てる |
| `01_利用者向け資料/90_サンプル/図書貸出/spec.md` | 23 | 依頼文から起こした仕様（FR-1〜FR-6・NFR） |
| `01_利用者向け資料/90_サンプル/図書貸出/CURRENT_STATE.md` | 23 | 引き継ぎメモ（フェーズ・次のタスク・判断待ち・未検証） |
| `01_利用者向け資料/note/yuki-aidd-kit導入ガイド.md` | 418 | note 掲載用の導入記事（本文・画像参照）。サブフォルダのため install / export では配らない |
| `01_利用者向け資料/note/yuki-aidd-kit導入ガイド.html` | 321 | 同記事のプレビュー（.md から生成） |
| `01_利用者向け資料/note/01_cover.png` | - | 記事の画像 |
| `01_利用者向け資料/note/02_concept.png` | - | 記事の画像 |
| `01_利用者向け資料/note/03_architecture.png` | - | 記事の画像 |
| `01_利用者向け資料/note/05_install.png` | - | 記事の画像 |
| `01_利用者向け資料/note/06_verify.png` | - | 記事の画像 |
| `01_利用者向け資料/note/10_guide.png` | - | 記事の画像 |
| `01_利用者向け資料/note/12_export.png` | - | 記事の画像 |
| `01_利用者向け資料/note/15_trace_check.png` | - | 記事の画像 |
| `01_利用者向け資料/note/17_sample_app.png` | - | 記事の画像 |
| `01_利用者向け資料/note/18_vmodel.png` | - | 記事の画像 |
| `01_利用者向け資料/note/19_daily_flow.png` | - | 記事の画像 |
| `01_利用者向け資料/note/20_token_audit.png` | - | 記事の画像 |

---

## 05_プロジェクト管理/・06_保守者向け/（14件・1,497行）— 計画と保守者専用（配布しない）。`06_保守者向け/01_内部仕様/` は本目録の対象外

| ファイル | 行 | 役割 |
|---|---|---|
| `ロードマップ.md` | 310 | **キット開発の作業台帳**。作業ルール5条と M1〜M14。未完チェック2件 |
| `保守者の傾向.md` | 106 | 保守者の指摘・要望の傾向 14 項目（出典・原文・現状・反映先）と反映しなかったものの理由 |
| `AUDIT-2026-07.md` | 114 | 資産監査の記録。判定軸・監査表3種・指摘 A-01〜A-09（ISTQB severity）・重複マップ D-01〜D-04・適用記録 |
| `要求仕様.md` | 86 | キット自体の要求文書。FR-01〜FR-10（+04a/08a/09a/09b/03a）と非機能（**互換性が最重要**） |
| `PROJECT-FIT-REPORT.md` | 48 | 実プロジェクト群への適合レポート（2026-06 時点）。Summary/Evidence/Recommendation |
| `EXTERNAL-REF-awesome-2026-09.md` | 162 | 外部参照調査（sindresorhus/awesome）。移植候補 E-1〜E-7・未カバー領域の一次資料 R-1〜R-7・メンター化の差分 G-1〜G-3・採用しない理由・未確認 X-1〜X-4 |
| `EXTERNAL-REF-github-2026-09.md` | 159 | GitHub 公開リポジトリの全件走査の要約（列挙 21,069。精読 2,563・説明文判定 18,506。有益 1,304。方法・欠落・次回作業） |
| `EXTERNAL-REF-github-2026-09.csv` | 21070 | 同・全件一覧（判定・理由コード・取り込み先・確認レベル） |
| `enrich_github_meta.py` | 45 | 同 CSV の ★/最終更新/アーカイブ有無を手元の `gh` と GraphQL で埋める（web セッションは他リポの API を使えないため） |
| `構想.md` | 47 | 目的・解決する問題6件・到達点3つ・Non-Goals・配置の2層・価値の判定基準 |
| `構成管理/構成管理計画書.md` | 70 | ISO 10007:2017 に沿った構成管理計画（識別・変更管理・状況の記録・監査・命名規則） |
| `構成管理/構成品目一覧.md` | 49 | 構成品目（CI-00〜）の台帳。品目 ID・配布区分・版の管理方法 |
| `02_設計判断の根拠/absolute-rules.md` | 165 | `rules/absolute-rules.md` の圧縮前原文（根拠・言い回し）。毎回は読まない（M16） |
| `02_設計判断の根拠/speed-harness.md` | 138 | `rules/speed-harness.md` の圧縮前原文（実測・失敗事例）と **H-6 の実測記録の追記先**（M16） |
| `02_設計判断の根拠/model-routing.md` | 31 | `rules/model-routing.md` 各行の根拠（一次情報の出典）と未確認事項（M16） |
| `02_設計判断の根拠/token-economics.md` | 39 | トークン消費の構造（実測・解釈・委譲の採算式・バイナリで確かめた設定・引用禁止の数値）。H-0・H-9・H-10 の根拠（7.1.0） |
| `06_保守者向け/学んだこと.md` | 215 | キット自身の AIDD プロセス改善ログ（Keep / Problem / Try。`02_共通/ひな形/lessons.md` は配布雛形で別物） |

---

## .github/workflows/（1件・70行）— キット自身の CI

| ファイル | 行 | 役割 |
|---|---|---|
| `.github/workflows/kit-ci.yml` | 72 | **workflow_dispatch のみ**（手動起動。PR / push では動かない）で 7 本の回帰テスト（hooks / trace-check / quality_harness / install / git-gates / check-docs / check-design）と `check-docs.sh` `check-design.sh` を `GATES_REQUESTED=1` で実行。レポートを artifact と step summary へ（M15 S5） |
