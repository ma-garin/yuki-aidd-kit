# 基本設計書（外部設計） — <プロジェクト名>

> 上位: `01-requirements.md`／下位: `03-detailed-design.md`
> **全 `REQ-F` を1つ以上の `BD` に割り当てる**（未割り当てゼロが出口基準）。
> 画面の具体値（色・タイポ・余白）は `skills/design-system` が真実源。ここでは参照のみ。
> 規模が小さく SDD の `plan.md` で足りる場合、本工程は省略して `plan.md` に集約してよい。

**日付**: YYYY-MM-DD ／ **状態**: ドラフト / 確定

## 全体構成

<!-- 構成図（箇条書き可）。処理の流れとデータの流れを分けて書く -->

```text
<利用者> → <画面/入口> → <処理モジュール> → <データストア>
                            ↘ <外部API>
```

## 構成要素（BD）

| ID | 要素 | 種別 | 責務（1行） | 対応 REQ |
|---|---|---|---|---|
| BD-001 |  | 画面 / モジュール / 外部I/F / データストア |  | REQ-F-001, REQ-N-001 |
| BD-0xx |  |  |  |  |

<!-- 採番したら `BD-0xx` を 3桁の連番に置き換える。全 REQ-F がいずれかの BD に割り当たっていること -->


## 画面設計（該当する場合）

| ID | 画面 | 主要要素 | 主要操作 | 遷移先 |
|---|---|---|---|---|
| BD-0xx |  |  |  |  |

- 視覚仕様（配色・タイポ・余白・状態表現）は `templates/design-system.md` と `skills/design-system` に従う。値をここに書き写さない
- 対象端末幅・レスポンシブ方針は `REQ-N` に従う

## データモデル

| エンティティ | キー | 主要項目 | 保存先 | version |
|---|---|---|---|---|
|  |  |  | localStorage / SQLite / JSON |  |

- スキーマ変更時のマイグレーション方針:
- export / import の対象範囲:

## 外部インターフェース

| ID | 相手 | 方式 | 入力 | 出力 | 失敗時の挙動 |
|---|---|---|---|---|---|
| BD-0xx |  | REST / ファイル / CLI |  |  | <!-- タイムアウト・4xx・5xx・不正レスポンス --> |

## 非機能要件の実現方式

| # | 対象 REQ-N | 実現方式 | 検証方法（ST で使う） |
|---|---|---|---|
| 1 | REQ-N-001 |  |  |

<!-- ID を第1セルに置くと「この文書での定義」とみなされる。他工程の ID は第2セル以降で参照する -->


## 技術選定

| 論点 | 候補 | 採用 | 理由 |
|---|---|---|---|
|  |  |  |  |

判断が重い/後戻りが高い選定は ADR（`templates/ADR-template.md`）を起こし、ここからは ADR 番号を参照する。

## リスクと回避策

| リスク | 影響 | 回避・軽減策 |
|---|---|---|

## 未解決事項（TBD）
