# 工程承認 — 第 __PHASE__ 工程 __PHASE_NAME__

> **この記録は `./scripts/check-approval.sh` が機械検査する。欄と見出しを消さない。**
> 判定基準の真実源は `skills/dev-lifecycle/references/phase-gates.md` 第 __PHASE__ 工程の出口基準。ここに基準を複製しない。
> **`approver` は人間しか埋めない。** AI は根拠・差し戻し事項・未確認事項までを書き、判定の申し送りをする（`/phase-review __PHASE__`）。

| 項目 | 値 |
|---|---|
| covers | __COVERS__ |
| reviewed_hash | 未記入 |
| git_head | 未記入 |
| approved_at | 未記入 |
| approver | 未記入 |

`reviewed_hash` は承認した瞬間の成果物の版。次で取得して貼る（パスの順序は結果に影響しない）:

```bash
./scripts/phase-hash.py __COVERS_ARGS__
```

**承認後に `covers` のファイルが1文字でも変われば、この承認は自動失効する。** 変更したら承認を取り直す。

## 判定

判定: 未記入

<!-- 承認 / 条件付き承認 / 差し戻し のいずれか1語に置き換える。未記入のままは「未承認」として扱われる -->

### 条件（「条件付き承認」のときのみ。1つでも未達なら承認ではない）

| # | 条件 | 検証方法 | 期限 |
|---|---|---|---|

## 根拠

`phase-gates.md` 第 __PHASE__ 工程の出口基準を項目ごとに埋める。
**確認した対象（ファイル・画面・件数）を書く。「確認した」だけは根拠ではない。**

| 出口基準 | 確認方法・確認した対象 | 結果 |
|---|---|---|

## 差し戻し事項

| ID | 内容 | 解消の検証方法 | 状態 |
|---|---|---|---|

- ID は `R-__PHASE__-1` から採番する
- **「解消の検証方法」が書けない指摘は差し戻さない**（受け手が完了を判定できないため）
- 状態は `未解消` / `解消済み`。**`未解消` が1件でも残っていれば承認にできない**

## 未確認事項（unknown）

<!-- 判定できなかった項目を1行1件で残す。空欄にして合格にしない -->

**ここに項目が残っている限り判定は「承認」にできない。** 判定不能を合格に数えない。
