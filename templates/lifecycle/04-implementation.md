# 実装記録 — <プロジェクト名>

> 上位: `03-detailed-design.md`／`T-ID` は SDD の `tasks.md` と同じ番号を使う（別採番しない）。
> 実装は `/plan` → `/implement`（実装モード）で行い、`templates/implement-profile.md` の規範に従う。
> **設計から逸脱したら、ここに理由を書いたうえで設計書へ逆同期する**（記録だけで終わらせない）。

## タスク一覧

| ID | タスク | 対応 DD | 依存 | 状態 | コミット / PR |
|---|---|---|---|---|---|
| T-001 |  | DD-001 | — | TODO / 実装中 / 完了 |  |
| T-0xx |  | DD-0xx | T-001 | TODO |  |

- 1タスク = 1セッションで完了する粒度
- 1コミット = 1論理変更（Conventional Commits）

---

## 作業ログ

### YYYY-MM-DD T-001

- **変更ファイル**: 
- **判断と理由**: 
- **設計からの逸脱**: なし / <逸脱内容と理由。設計書への反映: 済 / 未>
- **検証結果**: <!-- 実行したコマンドと結果。「動作確認した」でなくテスト実行の出力を書く -->
- **残課題**: 

---

## 秘密情報チェック

- [ ] APIキー・認証情報のハードコードなし（`scripts/pre-commit` または secret scan が通る）
- [ ] `.env` 等が `.gitignore` に含まれている

## 実装完了時の確認

- [ ] 全 `T-xxx` が完了
- [ ] 全 `DD-xxx` に対応する `T-xxx` が存在する（`./scripts/trace-check.sh` NG=0）
- [ ] 逸脱箇所を設計書へ逆同期済み
- [ ] `CURRENT_STATE.md` を更新済み
