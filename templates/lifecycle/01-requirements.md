# 要件定義書 — <プロジェクト名>

> 上位: `00-rfd.md`（決定済みの RFD だけを入力にする）／下位: `02-basic-design.md`
> **全要件に観測可能な受入基準を書く。** 書けない要件は要件になっていない。
> 非機能の既定値は `skills/nfr-standards` から引く（ここで再定義しない）。

**日付**: YYYY-MM-DD ／ **状態**: ドラフト / 承認済み

## 目的（1〜2文）

## 対象ユーザーと利用シーン

| ユーザー | シーン | 達成したいこと |
|---|---|---|
|  |  |  |

## スコープ / 対象外

- スコープ: 
- 対象外（Won't）: <!-- ここに書いた範囲は AI も探索しない -->

## 前提・制約・依存

| 区分 | 内容 |
|---|---|
| 技術制約 | <!-- 例: 単一HTML / GitHub Pages / ビルド不可 --> |
| 外部依存 | <!-- 例: API の無料枠上限、対象端末 --> |
| 禁止操作 | <!-- 例: 依存追加禁止、対象外ディレクトリへの書き込み禁止 --> |

## 機能要件（REQ-F）

| ID | 要件 | 優先度 | 由来 RFD | 受入基準（観測可能な合格条件） |
|---|---|---|---|---|
| REQ-F-001 |  | Must | RFD-001 |  |
| REQ-F-0xx |  | Should |  |  |

<!-- 採番したら `REQ-F-0xx` を 3桁の連番に置き換える。`-0xx` のままなら trace-check.sh の対象外 -->


- 優先度は MoSCoW（Must / Should / Could / Won't）
- 受入基準は UAT の手順にそのまま書き換えられる粒度で書く（例: ×「使いやすい」 ○「3タップ以内で保存でき、リロード後もデータが残る」）
- 「AかつB」は2要件に分割する

## 非機能要件（REQ-N）

ISO/IEC 25010 の品質特性で分類する。既定値は `skills/nfr-standards` のプロジェクト種別（PWA / 単一HTMLツール / Streamlit）から転記し、逸脱する場合のみ理由を書く。

| ID | 特性 | 要件 | 由来 RFD | 判定基準（測定条件付き） |
|---|---|---|---|---|
| REQ-N-001 | 性能効率性 |  | RFD-001 | <!-- 例: 3G相当(1.6Mbps)で初回操作可能まで3秒以内 --> |
| REQ-N-0xx | 使用性 |  |  |  |
| REQ-N-0xx | 信頼性 |  |  |  |
| REQ-N-0xx | セキュリティ |  |  |  |
| REQ-N-0xx | 保守性 |  |  |  |

該当する特性のみ採番する。判定基準は必ず測定条件つきで書く（「速いこと」は不可）。

## 用語定義

| 用語 | 定義 |
|---|---|

## 未解決事項（TBD）

<!-- 空であることが出口基準。残す場合は RFD へ差し戻すか、ADR で保留理由を明記する -->

## 承認

- [ ] 承認者（人間）: ____________ / 日付: ________
