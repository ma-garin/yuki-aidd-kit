# 実装モード プロファイル（implement-profile）

`.claude/mode` が存在する状態（実装モード）に適用する作業プロファイル。plan 確定後の実装フェーズで、再探索をやめて確定方針の実装に集中するための行動規範。`/implement` が有効化し、`block-explore.sh` hook が Read/Grep/Glob を物理ブロックする。

## 前提
- plan（`PLAN.md` 等）が確定済みであること。未確定なら実装モードに入らない（`/implement` が plan の存在をチェックし、無ければ中断する）
- 探索系ツール（Read / Grep / Glob）は実装モード中はブロックされる

## 行動規範
- **再探索しない**: 追加の Read/Grep/Glob を行わない。必要な情報は plan に記載済みという前提で進める
- **plan 準拠**: plan に列挙した対象ファイル・手順から逸脱しない
- **小さく実装 → 軽量テスト**: 1論理変更ごとにコミット。実装直後に軽量テスト、UI変更はブラウザ/スクリーンショット確認を省略しない
- **完了判定**: 「動いた」で終わらせず done-gate を通す
- **解除**: 実装完了後は `/plan` で `.claude/mode` を削除し、探索許可状態へ戻す

## 止まる条件（止まるのは失敗ではなく規約どおりの動作）

| 条件 | 止まる前にすること |
|---|---|
| plan の Step を最後まで終えた | 完了報告（報告の型 4 項目） |
| 同じゲート・検査・テストが 3 回連続で赤（`rules/absolute-rules.md` A-12） | 対象・失敗したゲート・3 回の修正内容・推定原因・次に試すことを報告 |
| plan に無い判断が要る（列名・規格の章名・仕様の解釈が未定義） | 推測で埋めない。質問を 1 行で書く |
| 規格・仕様の内容を推測で補う必要が生じた（A-5） | 「未確認」と書き、確かめ方を添える |
| 見込み往復数を超えた（A-2 / H-6） | 原因 1 行と短縮案 |
| ゲート定義・テスト・しきい値を弱めないと通らない | 変えずに止まる。変更が必要なら理由を報告して判断を仰ぐ（`floor-guard.py` が commit を止める。正当な変更は `Floor-Guard-Allow: <理由>` を commit メッセージに） |

止まるときは**進めた分をコミットし、`CURRENT_STATE.md` を最新にしてから**報告する。手段が塞がれただけ（権限・403・ツール不在）なら止まらず代替を取る（A-11）。

## 言い訳と事実（止まる条件を飛ばすときに頭に浮かぶ言葉。出所: agent-skills の Common Rationalizations）

| 頭に浮かぶ言い訳 | 事実 |
|---|---|
| 「小さい変更だからテストは後で」 | 後は来ない。1 論理変更 1 コミットの単位でテストまで含める。`floor-guard.py` は assert 減・skip 追加を止める |
| 「このしきい値は厳しすぎるだけ。少し下げれば通る」 | 通すために下げるのはゲートの無効化（A-12）。下げる根拠があるなら `Floor-Guard-Allow: <理由>` に書いて人が判断する |
| 「plan に無いが自明だから進める」 | 自明なら 1 行の質問に 1 行で答えが返る。推測で埋めた判断は後工程で全部やり直しになる（工程承認は版に縛られる） |
| 「テストは通った。動いている」 | テストが通った＝完了ではない（`rules/functional-integrity.md`）。実行経路と実機の全状態を見るまで「効いている」と言わない |
| 「もう 1 回だけ直せば通る」（3 回目の赤の後） | 4 回目以降は原因の特定でなく通し方の探索になる。5 項目の報告を出して止まる |
| 「規格の章番号は多分これ」 | 一次資料で確認していないものは「未確認」と書く（A-5）。確かめ方を添えれば人が 1 分で埋められる |
| 「検査が緑だから文書も最新」 | 検査は行数・件数・参照しか見ない。変更したファイル名で文書を grep する（`check-docs.sh --only-changed`） |
| 「保守者は忙しいから聞かずに決める」 | 決めてよいのは手戻りが起きない範囲だけ（A-7）。それ以外は選択肢 2 つ＋推奨で 1 往復に収める |

## 逸脱時の手順
plan に無い探索や方針変更が必要になったら:
1. 実装を止める
2. `/plan` で実装モードを解除（`.claude/mode` を削除）
3. 調査して方針を更新（PLAN.md を修正）
4. `/implement` で再び実装モードに入る
