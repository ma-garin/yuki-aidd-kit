# CURRENT_STATE — library-loan（社内図書館 貸出管理のモック）

## フェーズ
モック（HTML）完成。本実装は未着手。

## 直近の完了（2026-09-17）
- `library-loan.html`: 5 画面（ダッシュボード / 蔵書 / 貸出中 / 利用者 / 設定）を単一 HTML で。データは localStorage（`library-loan:data` schema v1）
- 貸出（モーダル・冊数上限のエラー）／返却（確認ダイアログ）／失敗の再現トグル／CSV 書き出し／ライト・ダーク／360px
- `check-design.sh .` NG=0、Playwright で 14 状態を確認（横スクロールなし・console error なし）

## 次のタスク
1. Excel 台帳の列と画面項目の対応を利用者（図書係）と確認する
2. 本実装の技術選定（単一 HTML のまま localStorage 共有不可 → サーバ保存が要るか）
3. 認証（誰が貸出操作できるか）の要否

## 判断待ち
- 延滞の通知手段（メール / Teams / 一覧だけ）
- 貸出期間・冊数上限の既定値（モックは 14 日・3 冊）

## 既知の問題・未検証
- Excel 取り込みは UI のみ（未実装）
- 利用者の追加・編集は表示のみ
- 「失敗時」はサーバ無しのため設定画面のトグルで再現（本物の通信失敗は未検証）
