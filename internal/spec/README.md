# internal/spec/ — yuki-aidd-kit 現況仕様書（保守者専用・配布しない）

このディレクトリは、**キット全体を1ファイルも省略せず読み切った結果の記録**である。
前提知識ゼロのモデル（人間でも AI でも）が、リポジトリ本体を読む前にここを読むだけで
「何が・どこに・なぜあるか」を把握し、そのまま作り込みを続けられる状態を維持する。

**初回記録日**: 2026-09-16 ／ **最終更新**: 2026-09-17（M15・M16 反映） ／ **対象リビジョン**: PR-A（#14）＋ PR-B の head
**対象範囲**: `internal/spec/` を除く全 126 ファイル・10,827 行（数値はすべて実測）
※ 初回記録時点の本体は 10,804 行。`INDEX.md` と `README.md` に `internal/spec/` への導線を追加した分（+23 行）を含む

---

## 読む順序

| # | ファイル | 何が書いてあるか | 行数目安 |
|---|---|---|---|
| 0 | `00-overview.md` | キットの目的・思想・配置の2層・版歴 | 中 |
| 1 | `01-inventory.md` | **全ファイル目録**（パス・行数・役割1行） | 大 |
| 2 | `02-architecture.md` | 読み込み経路・真実源マップ・発火機構・依存グラフ | 中 |
| 3 | `03-skills.md` | 20スキルの目的・トリガー・要点・委譲先 | 大 |
| 4 | `04-commands-hooks.md` | 18コマンド・13hooks・settings.json・実装モードの状態機械 | 中 |
| 5 | `05-scripts.md` | 14スクリプトの仕様（引数・出力・終了コード・回帰テスト） | 中 |
| 6 | `06-templates.md` | templates 31本 + `templates/github/workflows/` 4本 | 中 |
| 7 | `07-rules-docs.md` | rules 3本（A/H/機能完全性）・docs 8本・ルート4ファイル | 中 |
| 8 | `08-quality-gates.md` | 品質ゲートの全体像（L1〜L4・機械ゲート・ID体系・DoD） | 中 |
| 9 | `09-findings.md` | **現況の残課題**（実測値と証拠つき） | 中 |
| 10 | `10-backlog.md` | **作り込みバックログ**（決定済みの実行順・完了条件・検証手順） | 中 |
| 11 | `11-target-operating-model.md` | **移行先の運用条件（Pro＋Sonnet＋Codex）と一次情報・設計含意** ← キットの「なぜ」の最上位 | 中 |
| 12 | `12-design-framework.md` | デザインシステムの現状棚卸しと作り込み計画（DS-1〜DS-6） | 中 |

急ぐときは `11` → `10` → `02` の3本でよい（**目的 → 実行順 → 構造**）。実装に入るなら該当する `03`〜`08` を開く。

---

## このディレクトリの位置づけ（重要）

キット本体は「**同一情報の真実源は1箇所**」を規約にしている（`internal/PRD.md` 保守性 NFR）。
`internal/spec/` はそれを壊さないために、次の線を引く。

| 区分 | 置き場所 | internal/spec/ での扱い |
|---|---|---|
| **設計値・規約の定義**（色・トークン・ゲート基準・ID 体系・ECC 対応） | 本体（`skills/` `rules/` `docs/ECC-ASSET-MAP.md`） | **再定義しない。参照のみ**。値を写すときは「実測の引用」と明記する |
| **現況の事実**（どのファイルが何行で何をするか、どこが矛盾しているか） | `internal/spec/` | ここが真実源。本体には書かない |
| **これからやること** | `internal/spec/10-backlog.md` ↔ `internal/Roadmap.md` | Roadmap がマイルストーン台帳、`10-backlog.md` が未整理の候補置き場。**採用したら Roadmap へ昇格させ、backlog からは消す** |

矛盾を見つけたら、本体が正。`internal/spec/` を直す。ただし **本体どうしが矛盾している場合は `09-findings.md` に記録する**（勝手に片方へ寄せない）。

---

## 更新規約

1. **セッション開始時**: `internal/spec/README.md` → 必要なファイルだけ開く。全部読まない
2. **本体を変更したら、同じコミットで `internal/spec/` を更新する**。分けると必ず乖離する
3. 更新時は**実測値で書く**。`wc -l` を取らずに行数を書かない（`09-findings.md` の陳腐化はこれを怠って起きた）
4. 残課題を見つけたら `09-findings.md` に **ID・対象ファイル:行番号・evidence・severity（ISTQB）** で追記する。evidence の無い指摘は書かない
5. バックログを消化したら `10-backlog.md` から削り、`internal/Roadmap.md` の該当マイルストーンにチェックを入れる
6. 冒頭の「対象リビジョン」を更新する

### 更新漏れの検出

`internal/spec/` の数値が実体とズレていないかは、`10-backlog.md` の B-01（`ci/check-docs.sh`）で機械検証する計画。
実装されるまでは手動で次を回す。

```bash
# internal/spec/01-inventory.md の行数が実体と一致するか（暫定・目視）
find . -path ./.git -prune -o -path ./spec -prune -o -type f -print | sort | xargs wc -l | tail -1
```
